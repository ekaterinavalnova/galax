
// ✅ Конфиг: N дней для чёрного списка
const BLACKLIST_DAYS = 30;
const ITER = 200;
// ✅ Вынесенный номер WhatsApp в константу
const WHATSAPP_CONTACT = "+793********";
//const WHATSAPP_CONTACT = "+799********";
const DONOR_DESC = "Катя. Работаю дизайнером-архитектором, создаю уютные и эстетичные пространства, в которых сочетаются стиль и практичность. Увлекаюсь йогой, психологией и философией - люблю разбираться в тонкостях мышления и искать баланс. ";
//const DONOR_DESC = "Виктория. Играю на блок-флейте, занимаюсь рисованием, читаю научнпок, изучаю историю и сферу продаж";
const DONOR_NAME = "Катя";



 
if (!window.openAICache) window.openAICache = new Map();

// ✅ Функция для отправки запроса с ретраями и таймаутом
async function fetchWithRetry(url, options, retries = 3, timeout = 10000) {
	for (let i = 0; i < retries; i++) {
		try {
			const controller = new AbortController();
			const signal = controller.signal;
			const fetchPromise = fetch(url, { ...options, signal });

			// Устанавливаем таймер на прерывание запроса
			const timeoutId = setTimeout(() => controller.abort(), timeout);

			const response = await fetchPromise;
			clearTimeout(timeoutId); // Очищаем таймер, если запрос завершился успешно

			if (!response.ok) {
				throw new Error(`HTTP error! Status: ${response.status}`);
			}

			return await response.json();
		} catch (error) {
			console.error(`⚠️ Ошибка запроса (${i + 1}/${retries}): ${error.message}`);
			if (i === retries - 1) {
				throw new Error("❌ Исчерпаны попытки запроса к OpenAI API.");
			}
			await new Promise(res => setTimeout(res, 1000 * (i + 1))); // Увеличиваем задержку перед повтором
		}
	}
}
// ✅ Загружаем Supabase через ES-модуль с оптимизацией
window.initSupabase = (async () => {
	if (window.supabaseClient) return; // ✅ Если уже инициализировано — не повторяем

	console.log("⏳ [initSupabase] Загрузка Supabase...");

	if ("requestIdleCallback" in window) {
		requestIdleCallback(async () => {
			const { createClient } = await import("https://esm.sh/@supabase/supabase-js@2");
			window.supabaseClient = createClient(SUPABASE_URL, SUPABASE_KEY);
			console.log("✅ Supabase клиент инициализирован!");
		});
	} else {
		// ✅ Фолбэк для браузеров без requestIdleCallback
		setTimeout(async () => {
			const { createClient } = await import("https://esm.sh/@supabase/supabase-js@2");
			window.supabaseClient = createClient(SUPABASE_URL, SUPABASE_KEY);
			console.log("✅ Supabase клиент инициализирован!");
		}, 100);
	}
})();


// ✅ Оптимизированное ожидание Supabase
window.waitForSupabase = async function () {
	if (window.supabaseClient) return; // ✅ Уже готов

	console.log("⏳ [waitForSupabase] Ожидание инициализации Supabase...");

	// ✅ Если Supabase ещё не загружен, загружаем вручную
	if (!window.supabaseClient) {
		await window.initSupabase;
	}

	// ✅ Ждём, но не блокируем UI
	let attempts = 0;
	const maxAttempts = 10;
	let delay = 500;

	while (!window.supabaseClient) {
		if (attempts >= maxAttempts) {
			console.error("❌ [waitForSupabase] Supabase не загрузился!");
			throw new Error("Supabase не инициализировался.");
		}

		console.warn(`⏳ [waitForSupabase] Попытка ${attempts + 1}/${maxAttempts}...`);
		await new Promise(resolve => setTimeout(resolve, delay));

		attempts++;
		delay *= 1.5; // ✅ Увеличиваем паузу экспоненциально
	}

	console.log("✅ [waitForSupabase] Supabase загружен!");
};


async function getOpenAIResponse(fullPrompt) {
	try {
		if (!fullPrompt || fullPrompt.trim().length === 0) {
			console.error("❌ Ошибка: `fullPrompt` пустой. OpenAI не может ответить.");
			return ;
		}

		console.log("📡 Отправка запроса в OpenAI...");
		//console.log("📜 Отправляемый `fullPrompt`:", fullPrompt); // 🔍 Логируем, что именно отправляем

		const response = await fetchWithRetry("https://api.openai.com/v1/chat/completions", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
				"Authorization": `Bearer ${OPENAI_API_KEY}`
			},
			body: JSON.stringify({
				model: "gpt-4o-mini",
				messages: [{ role: "user", content: fullPrompt }], // Передаём fullPrompt
				temperature: 0.9,
				top_p: 0.8,
				max_tokens: 100
			})
		});

		if (!response || !response.choices || response.choices.length === 0) {
			console.warn("⚠️ OpenAI вернул пустой ответ. Используем fallback...");
			return ;
		}

		let bot_resp = response.choices[0].message.content.trim();
		console.log("📌 OpenAI ответ:", bot_resp);

		return bot_resp;

	} catch (error) {
		console.error("❌ Ошибка запроса к OpenAI:", error);
		return ;
	}
}



///////////////////////////////////////////////////////////////
/////////   🔥    функции для работы с blacklist   ////////////
///////////////////////////////////////////////////////////////

// ✅ Проверка, находится ли пользователь в blacklist
async function isUserBlacklisted(profileName) {
	await window.waitForSupabase();

	//==console.log(`🔍 [isUserBlacklisted] Проверяем, есть ли ${profileName} в blacklist...`);

	try {
		const { data, error } = await window.supabaseClient
			.from("blacklist")
			.select("user_nick")
			.eq("user_nick", profileName)
			.maybeSingle();

		if (error) {
			return false;
		}

		if (data && data.user_nick === profileName) {
			//console.warn(`⛔ [isUserBlacklisted] ${profileName} находится в blacklist!`);
			return true;
		}
		return false;
		
	} catch (err) {
		console.error("❌ [isUserBlacklisted] Фатальная ошибка при проверке blacklist:", err);
		return false;
	}
}



// ✅ Функция задержки (антибот-защита)
async function randomDelay() {
	await new Promise(resolve => setTimeout(resolve, Math.random() * (5000 - 2000) + 2000));
}




async function addToBlacklist(profileName, reason = "старая рега") {
	await window.waitForSupabase();

	try {
		const { error } = await window.supabaseClient
			.from("blacklist")
			.upsert([{ 
				user_nick: profileName, 
				reason: reason, 
				date_added: new Date().toISOString() 
			}], { onConflict: ["user_nick"] });

		if (error) {
			console.error("❌ Ошибка при добавлении в ЧС:", error);
			return false;
		}

		console.log(`✅ ${profileName} успешно добавлен в ЧС!`);
		return true;
	} catch (err) {
		console.error("❌ Фатальная ошибка при добавлении в ЧС:", err);
		return false;
	}
}



// ✅ Удаление пользователя из blacklist
async function removeFromBlacklist(profileName) {
	await window.waitForSupabase();

	const { error } = await window.supabaseClient
		.from("blacklist")
		.delete()
		.eq("user_nick", profileName);

	if (error) {
		console.error("❌ Ошибка при удалении из blacklist:", error);
		return false;
	}

	//==console.log(`✅ Пользователь ${profileName} удалён из blacklist.`);
	return true;
}



///////////////////////////////////////////////////////////////
///////// 🔥 Функции для работы с user_info_galaxy ////////////
///////////////////////////////////////////////////////////////
/**
 * Преобразует дату из формата "DD.MM.YYYY" в "YYYY-MM-DD"
 * @param {string} dateStr - Исходная дата в формате "DD.MM.YYYY"
 * @returns {string} Дата в формате "YYYY-MM-DD"
 */
function convertDate(dateStr) {
  const parts = dateStr.split(".");
  if (parts.length !== 3) {
	throw new Error("Неверный формат даты: " + dateStr);
  }
  // parts[0] - день, parts[1] - месяц, parts[2] - год
  const [day, month, year] = parts;
  return `${year}-${month}-${day}`;
}

// Пример использования:
const originalDate = "31.01.2022";


// ✅ Добавление нового пользователя
// ✅ Добавление нового пользователя в Supabase
async function addUser(profileName, userName, registrationDate, userCity, userAge) {
	await window.waitForSupabase();
	
	try {
		console.log(`🆕 Добавляем пользователя ${profileName}...`);
		const convertedDate = convertDate(registrationDate);
		console.log(convertedDate); // Ожидаемый вывод: "2022-01-31"
		userAge = userAge.replace(/[^\d]/g, "");

		// ✅ Добавляем пользователя в базу (или обновляем, если он уже есть)
		const { data, error } = await window.supabaseClient
			.from("user_info_galaxy")
			.upsert([{ 
				user_nick: profileName, 
				user_name: userName, 
				registration_date: convertedDate, 
				user_city: userCity, 
				user_age: userAge 
			}], { onConflict: ["user_nick"] });

		if (error) {
			console.error("❌ Ошибка при добавлении пользователя:", error);
			return false;
		}

		if (data && data.length > 0) {
			console.log(`✅ Пользователь ${profileName} успешно добавлен/обновлён!`);
			return true;
		}

		console.warn(`⚠️ Пользователь ${profileName} уже был в базе.`);
		return true;

	} catch (err) {
		console.error("❌ Ошибка в addUser():", err);
		return false;
	}
}


// ✅ Функция сохранения номера в phone_numbers
// ✅ Функция сохранения номера в Supabase
async function savePhoneNumber(userid, profileName, phoneNumber) {
	await window.waitForSupabase();

	try {
		console.log(`📞 Попытка сохранения номера ${phoneNumber} от ${profileName} (ID: ${userid})...`);

		// ✅ Добавляем или обновляем номер (если он уже есть)
		const { data, error } = await window.supabaseClient
			.from("phone_numbers")
			.upsert([{ userid, user_nick: profileName, phone_number: phoneNumber }], { onConflict: ["phone_number"] });

		// ✅ Логируем результат
		if (error) {
			console.error("❌ Ошибка при сохранении номера:", error);
			return false;
		}

		if (data.length > 0) {
			console.log(`✅ Номер ${phoneNumber} успешно сохранён в базе!`);
			return true;
		}

		// ✅ Если `upsert()` ничего не изменил, проверяем, есть ли этот номер в базе
		const { data: existingData, error: fetchError } = await window.supabaseClient
			.from("phone_numbers")
			.select("*")
			.eq("phone_number", phoneNumber)
			.limit(1);

		if (fetchError) {
			console.error("❌ Ошибка при проверке номера в базе:", fetchError);
			return false;
		}

		if (existingData.length > 0) {
			console.log(`ℹ️ Номер ${phoneNumber} уже был в базе.`);
			return true;
		}

		console.warn("⚠️ Номер не был сохранён по неизвестной причине.");
		return false;

	} catch (err) {
		console.error("❌ Ошибка при сохранении номера:", err);
		return false;
	}
}


// ✅ Функция проверки, есть ли номер в БД
async function isPhoneNumberExists(phoneNumber) {
	await window.waitForSupabase();
	try {
		const { count, error } = await window.supabaseClient
			.from("phone_numbers")
			.select("*", { count: "exact", head: true })
			.eq("phone_number", phoneNumber);
		return count > 0;
	} catch (err) {
		console.error("❌ Ошибка при проверке номера:", err);
		return false;
	}
}



// ✅ Исправленный `getUserInfo()`
async function getUserInfo(profileName) {
	await window.waitForSupabase();
	const { data, error } = await window.supabaseClient
		.from("user_info_galaxy")
		.select("*")
		.eq("user_nick", profileName)
		.maybeSingle();

	if (error) {
		console.error("❌ Ошибка загрузки userInfo:", error);
		return null;
	}

	//console.log(`✅ Данные пользователя из БД:`, data);
	return data;
}




// ✅ Обновление информации о пользователе
async function updateUser(profileName, updateData) {
	await window.waitForSupabase();
	try {
		const { count, error } = await window.supabaseClient
			.from("user_info_galaxy")
			.update(updateData)
			.eq("user_nick", profileName)
			.select("*", { count: "exact", head: true });
		return count > 0;
	} catch (err) {
		console.error("❌ Ошибка при обновлении данных пользователя:", err);
		return false;
	}
}


// ✅ Удаление пользователя
async function deleteUser(profileName) {
	await window.waitForSupabase();

	const { error } = await window.supabaseClient
		.from("user_info_galaxy")
		.delete()
		.eq("user_nick", profileName);

	if (error) {
		console.error("❌ Ошибка при удалении пользователя:", error);
		return false;
	}
	//==console.log("✅ Пользователь удалён.");
	return true;
}



///////////////////////////////////////////////////////////////
///////// 🔥 Функции для работы с Telegram_phrases ////////////
///////////////////////////////////////////////////////////////
/* async function getRandomGreeting() {
	if (!window.supabaseClient) {
		console.warn("⚠️ Supabase клиент не инициализирован!");
		return null;
	}

	try {
		// 📡 Запрос к Supabase через RPC
		const { data, error } = await window.supabaseClient.rpc("get_random_phrase");

		if (error) {
			console.error("❌ Ошибка Supabase:", error.message);
			return null;
		}

		if (!Array.isArray(data) || data.length === 0) {
			console.warn("⚠️ Нет данных в таблице `Telegram_phrases`.");
			return null;
		}

		console.log("✅ Получена случайная фраза:", data[0].message);
		return data[0].message;
	} catch (error) {
		console.error("❌ Ошибка при запросе случайной фразы:", error);
		return null;
	}
}
 */
async function showRandomMessage() {
	const message = await getRandomGreeting();
	if (!message) console.warn("⚠️ Нет фраз в базе.");
	return message || "⚠️ Фраз в базе нет.";
}

async function sendGreeting(profileName, userid) {
	console.log(`👋 Приветствуем нового пользователя: ${profileName}`);

	// Генерируем случайное приветственное сообщение
	const greeting = getRandomGreeting(); //"Привет! 😊 Как твои дела?";

	// Отправляем сообщение пользователю
	await handleSendMessageUserInput(greeting);

	// Сохраняем приветствие в историю чата
	const messages = [{ user: "bot", text: greeting, time: new Date().toISOString() }];
	await saveChatStatus(profileName, userid, messages, 1);

	return;
}


///////////////////////////////////////////////////////////////
/////////       🔥 Функции для работы с chats      ////////////
///////////////////////////////////////////////////////////////

// ✅ Получение истории чата из БД (если нет — создаём)
// ✅ Получение истории чата из БД (если нет — создаём)
async function getChatStatus(profileName) {
	//==console.log(`🔍 Проверяем чат для ${profileName}...`);
	await window.waitForSupabase();

	try {
		const { data, error } = await window.supabaseClient
			.from("chats")
			.select("userid, messages, stage")
			.eq("user_nick", profileName)
			.maybeSingle();

		if (error) throw error;

		return data || { userid: null, messages: [], stage: 1 };
	} catch (error) {
		console.error("❌ Ошибка в getChatStatus:", error);
		return { userid: null, messages: [], stage: 1 };
	}
}


function mergeUniqueMessages(oldMessages, newMessages) {
	if (!Array.isArray(oldMessages)) {
		console.error("Ошибка: oldMessages не массив, заменяем на пустой массив.");
		oldMessages = [];
	}

	const texts = new Set(oldMessages.map(msg => msg.text)); // Запоминаем существующие тексты

	newMessages.forEach(msg => {
		if (!texts.has(msg.text.trim())) {  // Проверяем без лишних пробелов
			oldMessages.push(msg);
			texts.add(msg.text.trim());  // Добавляем в Set
		}
	});

	return oldMessages;
}

 
async function saveChatStatus(profileName, userid, messages, stage) {
	if (!profileName || !userid) {
		console.warn("⚠️ Ошибка: Пустые данные для сохранения!");
		return false;
	}
	console.log("📤 Пытаемся сохранить чат:", profileName, messages);

	if (!Array.isArray(messages) || messages.length === 0) {
		console.warn(`⚠️ Чат ${profileName} пуст, обновление не требуется.`);
		return true;
	}
	

	try {
		await window.waitForSupabase();
		// ✅ Загружаем текущие сообщения из базы
		const { data, error: fetchError } = await window.supabaseClient
			.from("chats")
			.select("messages, stage")
			.eq("user_nick", profileName)
			.maybeSingle();

		if (fetchError) throw fetchError;

		let updatedMessages = [];

		// ✅ Если в базе уже есть сообщения — загружаем их
		if (data && data.messages) {
			updatedMessages = [...data.messages];
		}

		// ✅ Добавляем только новые сообщения (исключаем дубли)
		messages.forEach(msg => {
			if (!updatedMessages.some(existing => 
				existing.user === msg.user && 
				existing.text === msg.text && 
				existing.time === msg.time)) {
					;
			}
		});
		console.log("!!!!! UPSERT")
		//console.log(messages)
		if (!Array.isArray(messages) || messages.length === 0) {
			console.warn("⚠️ Сообщения пусты, запись в базу не требуется.");
			return;
		}
		
		// ✅ Сохраняем обновлённый JSON в базу
		const { error } = await window.supabaseClient
			.from("chats")
			.upsert([{ 
				user_nick: profileName, 
				userid, 
				messages: messages, 
				stage 
			}], { onConflict: ["user_nick"] });

		if (error) throw error;

		console.log(`✅ Чат ${profileName} успешно обновлён, добавлено ${messages.length} новых сообщений.`);
		return true;

	} catch (error) {
		console.error("❌ Ошибка в saveChatStatus():", error);
		return false;
	}
}









// ✅ Удаление чата
async function deleteChat(profileName) {
	//==console.log(`🗑️ Удаляем чат с ${profileName}...`);
	await window.waitForSupabase();

	try {
		const { error } = await window.supabaseClient
			.from("chats")
			.delete()
			.eq("user_nick", profileName);

		if (error) throw error;

		//==console.log(`✅ Чат с ${profileName} удалён.`);
		return true;
	} catch (error) {
		console.error("❌ Ошибка в deleteChat:", error);
		return false;
	}
}




///////////////////////////////////////////////////////////////



///////////////////////////////////////////////////////////////
/////////   🔥 Функции для работы с instructions   ////////////
///////////////////////////////////////////////////////////////

// ✅ Исправленная функция загрузки инструкции
async function getInstruction() {
	await window.waitForSupabase();

	//==console.log("📜 Загружаем инструкцию...");

	try {
		const { data, error } = await window.supabaseClient
			.from("instructions")
			.select("content")
			.eq("name", "katya_behavior")
			.maybeSingle(); // Используем maybeSingle()

		if (error) {
			console.error("❌ Ошибка при загрузке инструкции:", error);
			return null;
		}

		if (!data || !data.content || typeof data.content !== "string") {
			console.warn("⚠️ Инструкция отсутствует или имеет неверный формат!");
			return null;
		}

		//==console.log("📜 Инструкция успешно загружена!");
		return data.content.trim();
	} catch (err) {
		console.error("❌ Фатальная ошибка при загрузке инструкции:", err);
		return null;
	}
}

// ✅ Обновление инструкции
async function updateInstruction(newContent) {
	await window.waitForSupabase();

	const { data, error } = await window.supabaseClient
		.from("instructions")
		.update({ content: newContent })
		.eq("name", "katya_behavior");

	if (error) {
		console.error("❌ Ошибка при обновлении инструкции:", error);
		return false;
	}
	//==console.log("✅ Инструкция обновлена.");
	return true;
}

// ✅ Удаление инструкции
async function deleteInstruction() {
	await window.waitForSupabase();

	const { error } = await window.supabaseClient
		.from("instructions")
		.delete()
		.eq("name", "katya_behavior");

	if (error) {
		console.error("❌ Ошибка при удалении инструкции:", error);
		return false;
	}
	//==console.log("✅ Инструкция удалена.");
	return true;
}


async function isCityInDatabase(cityName) {
	await window.waitForSupabase();

	try {
		const { count, error } = await window.supabaseClient
			.from("cities")
			.select("*", { count: "exact", head: true }) // ✅ Оптимизация запроса
			.or(`city_ru.eq.${cityName},city_en.eq.${cityName}`);

		if (error) {
			console.error("❌ Ошибка при запросе к Supabase:", error);
			return false;
		}

		return count > 0;
	} catch (err) {
		console.error("❌ Фатальная ошибка при запросе в Supabase:", err);
		return false;
	}
}



function parseUrlParams(url) {
	try {
		const params = new URL(url).searchParams;
		let userid = params.get("userID");
		const password = params.get("password");

		return { userid, password };
	} catch (error) {
		console.error("❌ Ошибка парсинга URL:", error);
		return { userid: null, password: null };
	}
}


// 📂 Часть 3: Работа с Supabase (чёрный список, обновление статусов)

// == Работа с Supabase == //

async function updateStatusToTrue(id, column) {
	await waitForSupabase();
	let { error } = await window.supabaseClient
		.from("socstarters")
		.update({ [column]: true })
		.eq("id", id);

	if (error) console.error(`❌ Ошибка обновления ${column} для ID ${id}:`, error.message);
}

// ✅ Проверка пользователя в чёрном списке
async function checkUserBlacklist(profileName) {
	await waitForSupabase();
	try {
		let { data, error } = await window.supabaseClient
			.from("blacklist")
			.select("user_nick")
			.filter("user_nick", "eq", profileName) // Используем `filter()`
			.maybeSingle(); // Используем `maybeSingle()`, чтобы избежать ошибок


		if (error && error.code !== "PGRST116") {
			console.error("❌ Ошибка проверки чёрного списка:", error.message);
			return false;
		}
		//==console.log(`📡 Проверка ЧС для '${profileName}':`, data);

		return data !== null; 
	} catch (e) {
		console.error("❌ Неожиданная ошибка при проверке чёрного списка:", e);
		return false;
	}
}






function cleanMessageText(text) {
	if (!text || typeof text !== "string") return "";

	// Удаляем номера телефонов (заменяем на пустую строку)
	return text.replace(/(?:\+7|8)?[\d\s\-\(\)]{9,}/g, "").trim();
}

function isRussianText(text) {
	if (!text || typeof text !== "string") return false;
	
	// ✅ Проверяем, является ли сообщение номером телефона
	const phoneRegex = /(?:\+7|8)?[\d\s\-\(\).]{9,}/;
	if (phoneRegex.test(text)) {
		console.log("📞 Сообщение — номер телефона. Продолжаем работу.");
		return true;
	}
	
	// Убираем номера перед проверкой языка
	const cleanedText = cleanMessageText(text);

	// Если после очистки текст пустой, пропускаем проверку
	if (!cleanedText) return true;

	// Убираем только пробелы и цифры, оставляем буквы
	const filteredText = cleanedText.replace(/[\s\d]/g, "");

	if (!filteredText.length) return false;

	// Считаем процент кириллических символов
	const cyrillicCount = (filteredText.match(/[а-яА-ЯёЁ]/g) || []).length;
	const percentage = (cyrillicCount / filteredText.length) * 100;

	return percentage >= 70; // Если 70%+ символов — текст русский
}




function sleep(ms) {
	return new Promise((resolve) => setTimeout(resolve, ms));
}

async function randomPause(min = 7000, max = 20000) {
	const randomTime = Math.floor(Math.random() * (max - min + 1)) + min;
	await sleep(randomTime);
}






// ✅ Безопасный клик по элементу
function safeClick(element, description) {
	if (element && typeof element.click === "function") {
		//==console.log(`🖱️ Кликаем по: ${description}`);
		element.dispatchEvent(new Event('click', { bubbles: true, cancelable: true }));
		return true;
	} else {
		console.warn(`⚠️ Элемент '${description}' не найден или не кликабелен.`);
		return false;
	}
}

// ✅ Функция паузы (дубликат, но может использоваться в других частях кода)
function sleep(ms) {
	return new Promise((resolve) => setTimeout(resolve, ms));
}

// ✅ Ожидание элемента внутри iframe
// ✅ Улучшенная версия ожидания элемента в iframe
function waitForElementInIframe(iframe, selector, timeout = 3000) {
	return new Promise((resolve, reject) => {
		let iframeDoc = getUpdatedIframeContext();
		
		if (!iframeDoc) {
			console.warn("⚠️ iframe не найден! Повторяем попытку...");
			setTimeout(() => {
				iframeDoc = getUpdatedIframeContext(); // 🔄 Пробуем обновить iframe
				if (!iframeDoc) {
					console.error("❌ iframe недоступен даже после повторной попытки.");
					return reject(new Error("Iframe недоступен."));
				}
			}, 500);
		}

		const interval = 100; 
		let elapsedTime = 0;

		const checkElement = () => {
			if (!iframeDoc) {
				console.error("❌ Ошибка: document в iframe не найден.");
				reject(new Error("Iframe document недоступен."));
				return;
			}

			const element = iframeDoc.querySelector(selector);
			if (element) {
				console.log(`✅ Элемент '${selector}' найден в iframe.`);
				resolve(element);
			} else if (elapsedTime >= timeout) {
				console.error(`⏳ Таймаут: Элемент '${selector}' не найден в iframe.`);
				reject(new Error(`Таймаут: Элемент не найден: ${selector}`));
			} else {
				elapsedTime += interval;
				setTimeout(checkElement, interval);
			}
		};

		checkElement();
	});
}


// ✅ Выполнение правого клика по элементу
function performRightClick(element, description) {
	if (!element) {
		//console.warn(`⚠️ Элемент для правого клика не найден: ${description}`);
		return false;
	}

	const event = new MouseEvent("contextmenu", {
		bubbles: true,
		cancelable: true,
		view: window,
		button: 2,
	});

	element.dispatchEvent(event);
	//==console.log(`🖱️ Правый клик выполнен: ${description}`);
	return true;
}




// ✅ Поиск элемента по XPath
function findElementByXPath(xpath, context = document) {
	return document.evaluate(
		xpath,
		context,
		null,
		XPathResult.FIRST_ORDERED_NODE_TYPE,
		null
	).singleNodeValue;
}


// function getRandomMessage(messages) {
//     if (messages.length === 0) {
//         throw new Error("No messages left to send.");
//     }
//     return messages.splice(Math.floor(Math.random() * messages.length), 1)[0];
// }

function getUserMessage() {
	let userMessage = "";
	while (!userMessage.trim()) {
		userMessage = prompt("Enter the message to send:");
		if (userMessage === null) {
			console.warn("Message input was cancelled. Exiting script.");
			return null;
		}
	}
	return userMessage;
}

/* function getRandomGreeting() {
	const greetings = ["Привет", "Приветик!", "Хей", "Привет!", "Привет )))", "Приветик ))))"];
	return greetings[Math.floor(Math.random() * greetings.length)];
} */



// ✅ Ожидание элемента внутри iframe
function waitForElementInIframe(iframe, selector, timeout = 3000) {
	return new Promise((resolve, reject) => {
		let iframeDoc = getIframeDocument();
		if (!iframeDoc) return console.warn("⚠️ iframe не найден!");
		
		let elapsedTime = 0;

		const checkElement = () => {
			if (!iframeDoc) {
				reject(new Error("Iframe document not accessible."));
				return;
			}
			try {
				const page_error = findElementByXPath('//*[@id="content"]/div/div[text()="Something went wrong..."]', iframeDoc);
				if (page_error) {   
					// Добавляем в чёрный список
					addToBlacklist(profileName, "Something went wrong...");
					
					randomPause(1040, 2036);
					
					// Вызываем логику удаления, но продолжаем цикл
					//handleUser(iframeDoc, profileName);
					
					// Указываем, что дальнейшая обработка не нужна
					shouldContinue = false;
					return;
				}
			} catch (error) {};
			
			const element = iframeDoc.querySelector(selector);
			if (element) {
				resolve(element);
			} else if (elapsedTime >= timeout) {
				console.error(`Timeout: Element not found within iframe: ${selector}`);
				reject(new Error(`Timeout: Element not found: ${selector}`));
			} else {
				elapsedTime += 100;
				setTimeout(checkElement, 100);
			}
		};

		checkElement();
	});
}

function isSimilarMessage(lastMessage, newMessage, threshold = 0.8) {
	if (!lastMessage || !newMessage) return false;

	function similarity(str1, str2) {
		const set1 = new Set(str1.toLowerCase().split(" "));
		const set2 = new Set(str2.toLowerCase().split(" "));
		const intersection = new Set([...set1].filter(x => set2.has(x)));
		return intersection.size / Math.max(set1.size, set2.size);
	}

	return similarity(lastMessage, newMessage) >= threshold;
}

function analyzeLastUserMessages(chatMessages) {
	if (!Array.isArray(chatMessages) || chatMessages.length === 0) {
		console.warn("⚠️ История сообщений пуста, настроение пользователя не определено.");
		return "neutral"; 
	}

	// ✅ Берём последние 3 сообщения пользователя
	const userMessages = chatMessages.filter(msg => msg.user === "user").slice(-3);

	if (userMessages.length === 0) {
		console.warn("⚠️ У пользователя нет сообщений, настрой не определён.");
		return "neutral";
	}

	const lastUserMessages = chatMessages
		.filter(msg => msg.user === "user" && msg.text?.trim().length > 0)
		.map(msg => msg.text.trim());

	if (lastUserMessages.length === 0) {
		console.warn("⚠️ analyzeLastUserMessages(): У пользователя нет сообщений.");
		return "neutral";
	}

	const lastMessage = lastUserMessages.at(-1); // Берём последнее сообщение пользователя
	if (!lastMessage) {
		console.warn("⚠️ analyzeLastUserMessages(): Последнее сообщение не найдено.");
		return "neutral";
	}

	if (isQuestion(lastMessage)) {
		console.log("❓ Пользователь задал вопрос.");
		return "curiosity";
	}
	const combinedText = userMessages.map(msg => msg.text.toLowerCase()).join(" ");

	// ✅ 1️⃣ Проверяем интерес к WhatsApp / Телеграму
	// ✅ 1️⃣ Проверяем интерес к WhatsApp / Телеграму
	if (/телеᴦрам|телеграм|вайбер|перейти|номер|вотс|в[оа]т?[сцз]?[ао]?[пб]+|в[оа]ц[ао]?[пб]+|вос?ап+|вас?ап+|вотц[ао]п+|ватц[ао]п+|ватз[ао]п+/i.test(combinedText)) {
			console.log("📌 Пользователь интересуется WhatsApp.");
			return "telegram_interest";
	}

	// ✅ 2️⃣ Проверяем, подтвердил ли пользователь переход
	if (/\b(давай|не против|перейдём|можно|да|хорошо|ок)\b/i.test(combinedText)) {
		console.log("📌 Пользователь согласился на WhatsApp.");
		return "confirmed_telegram";
	}

	// ✅ 3️⃣ Проверяем, отказался ли пользователь
	if (/не хочу|не буду|не интересно|против|отказ/i.test(combinedText)) {
		console.log("📌 Пользователь отказался.");
		return "refusal";
	}

	// ✅ 4️⃣ Проверяем, если это просто приветствие
	if (/^(привет|здравствуйте|приветик|Приветик|приветики|Приветики|Привет|добрый день|добрый вечер|хай|хей|йоу)/i.test(lastMessage)) {
		console.log("📌 Пользователь просто поздоровался.");
		return "greeting";
	}

	// ✅ 5️⃣ Анализ эмоций
	const moods = {
		"happiness": /прекрасно|супер|великолепно|классно|обожаю|весело|счастлив/i,
		"sadness": /грустно|печально|депрессия|тяжело|разочарован|неудачный день/i,
		"anger": /злюсь|раздражён|бесит|ненавижу|черт|как же бесит/i,
		"anxiety": /переживаю|волнуюсь|тревожно|не знаю, что делать|стресс/i,
		"tiredness": /устал|измотан|невыспался|голова кругом|слишком много дел/i,
		"excitement": /вдохновляет|хочу попробовать|новые идеи|это интересно/i,
		"sarcasm": /конечно, конечно|как неожиданно|ну да, ну да|ага, рассказывай/i
	};

	for (let mood in moods) {
		if (moods[mood].test(combinedText)) {
			console.log(`📌 Распознано настроение: ${mood}`);
			return mood;
		}
	}

	// ✅ 6️⃣ Проверяем, игнорирует ли пользователь бота (если в 3 последних сообщениях нет реакции на предыдущие)
	const botMessages = chatMessages.filter(msg => msg.user === "bot").slice(-3);
	if (botMessages.length > 0 && userMessages.length > 0) {
		const lastBotMessage = botMessages[botMessages.length - 1].text.toLowerCase();
		if (!combinedText.includes(lastBotMessage) && lastBotMessage.length > 10) {
			console.log("📌 Пользователь, возможно, игнорирует бота.");
			return "user_ignoring_bot";
		}
	}

	// ✅ 7️⃣ Распознавание вопросов
/*     if (/\?$/.test(lastUserMessage)) {
		console.log("📌 Пользователь задал вопрос.");
		return "question";
	}     */
	// ✅ Если есть вопрос, указываем, что бот должен на него ответить


	// ✅ 8️⃣ Проверяем смену темы (если последние сообщения сильно отличаются)
	if (userMessages.length >= 2) {
		const lastTopic = userMessages[userMessages.length - 1].text.toLowerCase();
		const prevTopic = userMessages[userMessages.length - 2].text.toLowerCase();
		if (lastTopic.length > 10 && prevTopic.length > 10 && !lastTopic.includes(prevTopic)) {
			console.log("📌 Пользователь сменил тему.");
			return "topic_change";
		}
	}

	console.log("📌 Обычное сообщение, продолжаем разговор.");
	return "neutral"; 
}


// 📂 Часть 4: Обработка сообщений

// == Обработка сообщений == //

// ==== 📌 Message Handling ==== //




async function handleSendMessageUserInput(message) {
		try {

			if (!message) {
				return;
			}

			let iframeDoc = null;
			let retryCount = 0;
			const maxRetries = 10;

			iframeDoc = getIframeDocument();
			if (!iframeDoc) return console.warn("⚠️ iframe не найден!");


			while (!iframeDoc && retryCount < maxRetries) {
				iframeDoc = getIframeDocument(); // Используем оптимизированную функцию
				retryCount++;
			}

			if (!iframeDoc) throw new Error("Failed to access iframe document after multiple retries.");

			// 🔥 **Проверка наличия элемента перед отправкой сообщения** 🔥
			const existingMessageXPath = "//div[@class='chat-message-meta is-own is-text']";
			const existingMessage = document.evaluate(
				existingMessageXPath,
				iframeDoc,
				null,
				XPathResult.FIRST_ORDERED_NODE_TYPE,
				null
			).singleNodeValue;

			
			//message = message.replace(/Telegram/g, "телеᴦрам");
			message = message.replace(/`${WHATSAPP_CONTACT}`./g, "`${WHATSAPP_CONTACT}`");
			message = message.replace(/WhatsApp/g, "вотcaп");
			const messageLines = message.replace(/@NL@/g, "\n").split("\n").filter(line => line.trim() !== ""); // Fix applied
			

			for (const line of messageLines) {
				let textArea;
				// 🔥 **Найти и сфокусироваться на textarea** 🔥
				while (!textArea && retryCount < maxRetries) {
					iframeDoc = getIframeDocument();
					if (!iframeDoc) return console.warn("⚠️ iframe не найден!");
					textArea = document.evaluate(
						"//textarea[@placeholder='Сообщение']",
						iframeDoc,
						null,
						XPathResult.FIRST_ORDERED_NODE_TYPE,
						null
					).singleNodeValue;
					retryCount++;
				}
				if (!textArea) { throw new Error(`Textarea not found using XPath after ${maxRetries} retries.`);
				}

				textArea.focus();
				await randomPause(903, 1693);
				textArea.value = "";
				textArea.dispatchEvent(new Event("input", { bubbles: true }));
				await new Promise(resolve => requestAnimationFrame(() => setTimeout(resolve, 1000))); // Даем браузеру время обновить DOM
				await randomPause(1303, 1693);

				// 🔥 **Ввод текста** 🔥
				for (const char of line) {
					textArea.value += char;
					const inputEvent = new InputEvent("input", { bubbles: true, cancelable: true, inputType: "insertText", data: char });
					textArea.dispatchEvent(inputEvent);
					//await randomPause(79, 113);
					await new Promise(resolve => setTimeout(resolve, Math.random() * 100 + 100)); // 100–300 мс
				}

				// 🔥 **Отправка сообщения** 🔥
				const sendButtonXPath = "//div[contains(@class, 'text_field_button send')]";
				const sendButton = document.evaluate(
					sendButtonXPath,
					iframeDoc,
					null,
					XPathResult.FIRST_ORDERED_NODE_TYPE,
					null
				).singleNodeValue;

				if (!sendButton) throw new Error(`Send button not found using XPath: ${sendButtonXPath}`);
				//sendButton.click();
				// ✅ Нажимаем Enter для отправки
				const enterEvent = new KeyboardEvent("keydown", { key: "Enter", bubbles: true });
				textArea.dispatchEvent(enterEvent);
				console.log("🚀 Сообщение отправлено!");
				await randomPause(2831, 10393);
			}
		} catch (error) {
			console.error("Error in handleSendMessageUserInput:", error);
		}
}

/**
 * Склеивает подряд идущие сообщения одного пользователя,
 * если между ними прошло меньше maxIntervalMs (по умолчанию 2 минуты).
 */
function combineConsecutiveUserMessages(messages, maxIntervalMs = 120000) {
	// Сортируем все сообщения по времени (на случай если массив не отсортирован)
	messages.sort((a, b) => new Date(a.time) - new Date(b.time));

	const result = [];
	for (let msg of messages) {
		// Если result пуст или последнее сообщение — от другого юзера / слишком старая реплика
		if (
			!result.length ||
			result[result.length - 1].user !== msg.user ||
			new Date(msg.time) - new Date(result[result.length - 1].time) > maxIntervalMs
		) {
			// Просто добавляем текущее сообщение
			result.push({ ...msg });
		} else {
			// Иначе объединяем с предыдущим
			// Допустим, склеиваем текст через перенос строки
			result[result.length - 1].text += "\n" + msg.text;
			// А время (time) сделаем временем последнего из них (чтобы «последнее» было более позднее)
			result[result.length - 1].time = msg.time;
		}
	}
	return result;
}



async function parseChatMessages() {
	let iframeDoc = getIframeDocument();
	if (!iframeDoc) return [];
  
	await new Promise(resolve => setTimeout(resolve, 1500));
	let chatWrappers = iframeDoc.querySelectorAll(".chat-message-wrapper");

	// ✅ Если в iframe чата нет сообщений — пробуем искать в `document`
	if (!chatWrappers.length) {
		chatWrappers = document.querySelectorAll(".chat-message-wrapper");
		if (!chatWrappers.length) {
			console.warn("⚠️ Чат пуст или не найден!");
			return [];
		}
	}
   


	let messages = [];
	for (let chat of chatWrappers) {
		const isBotMessage = chat.querySelector(".chat-message.is-own") !== null;
		const textElement = chat.querySelector(".chat-message-content-text span");
		const timeElement = chat.querySelector("div.chat-message-content-wrapper > div > div > div > time");

		if (!textElement || !timeElement) continue;

		const text = textElement.textContent.trim();
		let timestamp = timeElement.getAttribute("datetime") || timeElement.textContent.trim();

		// Если формат времени HH:MM → добавляем сегодняшнюю дату
		if (/^\d{1,2}:\d{2}$/.test(timestamp)) {
			const now = new Date();
			const [hours, minutes] = timestamp.split(":").map(Number);
			now.setHours(hours, minutes, 0, 0);
			timestamp = now.toISOString();
		}

		messages.push({
			user: isBotMessage ? "bot" : "user",
			text: text,
			time: timestamp
		});
	}

	//console.log(`✅ Загружено ${messages.length} сообщений (до объединения).`);

	// Добавляем строку объединения подряд идущих сообщений
	messages = combineConsecutiveUserMessages(messages, 2 * 60_000);

	console.log(`✅ Сообщений после объединения подряд идущих: ${messages.length}`);
	return messages.reverse(); // Возвращаем в обратном порядке, если нужно
}

async function clickFallback(retries = 1, waitTime = 3000) {
	const fallbackSelector =
		"#root > div > div.auth-user > div.app-content.mdc-drawer-app-content > div.browser.browser--auth-user > header > div > section.mdc-top-app-bar__section.mdc-top-app-bar__section--align-start > div:nth-child(1)";
	console.log("clickFallback");
	for (let attempt = 1; attempt <= retries; attempt++) {
		const fallbackElement = document.querySelector(fallbackSelector);
		if (!fallbackElement) {
			await randomPause(1231, 2393);
			continue;
		}
		fallbackElement.click();
		await randomPause(1931, 3093);
		if (document.querySelector("div.mail_chat_list_wrap")) {
			return;
		}
	}
}

async function clickNearMenu(retries = 1, waitTime = 3000) {
	
	
	if (document.querySelector("#root > div > div.auth-user > aside > div.mdc-drawer__content.drawer__content.scroll > nav > a:nth-child(2)").click()) {
		await randomPause(1231, 1993);
		return;
	}
}




async function scrollChatListDown() {
	//==console.log("🔽 Прокручиваем список чатов вниз...");
  
	let iframeDoc = getIframeDocument();
	if (!iframeDoc) return console.warn("⚠️ iframe не найден!");

	// 1️⃣ Получаем контейнер со списком чатов
	let chatListContainer = iframeDoc.querySelector(".mail_chat_list_wrap");
	if (!chatListContainer) {
		console.warn("⚠️ Контейнер чатов не найден!");
		return false;
	}

	let lastChatCount = chatListContainer.querySelectorAll(".mail_chat_item").length; // Число чатов до скролла
	//==console.log(`📊 Чатов до скролла: ${lastChatCount}`);

	for (let i = 0; i < 10; i++) { // 🔄 Делаем до 10 скроллов
		//==console.log(`🔄 Попытка скролла #${i + 1}...`);
		
		// 2️⃣ Пробуем `scrollIntoView()` к последнему чату
		let chatItems = chatListContainer.querySelectorAll(".mail_chat_item");
		let lastChat = chatItems[chatItems.length - 1]; // Берем последний чат

		if (lastChat) {
			lastChat.scrollIntoView({ behavior: "smooth", block: "end" });
			//==console.log("✅ Скролл через scrollIntoView() сработал!");
		} else {
			console.warn("⚠️ Последний чат не найден!");
		}

		await new Promise(resolve => setTimeout(resolve, 2000)); // Ждем 500 мс

		// 3️⃣ Проверяем, появились ли новые чаты
		let currentChatCount = chatListContainer.querySelectorAll(".mail_chat_item").length;
		//==console.log(`📊 Чатов после скролла: ${currentChatCount}`);

		if (currentChatCount === lastChatCount) {
			console.warn("⚠️ Новые элементы не загружаются, прекращаем скроллить.");
			break; // 🚫 Прерываем цикл
		}

		lastChatCount = currentChatCount; // Обновляем счетчик
	}

	//==console.log("✅ Скроллинг завершен.");
	return true;
}


// 📂 Часть 5: Работа с iframe

// ✅ Оптимизированная функция обновления iframe-контекста
function getUpdatedIframeContext() {
	let iframeDoc = getIframeDocument();
	if (!iframeDoc) return null;

	return { updatedIframe: document.querySelectorAll("iframe")[1], iframeDoc };
}


function getIframeDocument() {
	const iframe = document.querySelectorAll("iframe")[1]; // Берём второй iframe
	return iframe ? iframe.contentDocument || iframe.contentWindow.document : null;
}

async function handleUser(iframeDoc, profileName) {
	await randomPause(540, 1336);
	try {
		console.warn("⚠️ Проверяем, находится ли пользователь в ЧС...");
		const isBlacklisted = await checkUserBlacklist(profileName);
		if (!isBlacklisted) {
			//==console.log(`✅ Пользователь '${profileName}' не в ЧС, диалог не удаляем.`);
			return; 
		}

		//==console.log(`🔍 Ищем непрочитанные сообщения с ником '${profileName}'...`);
		const unreadMessageSelector = 
			"div.mail_chat_list_wrap > div.mail_chat_item > div.mail_chat_item__content_grid > div:nth-child(2)";
		const allUnreadMessages = iframeDoc.querySelectorAll(unreadMessageSelector);
		//==console.log(`✅ Найдено ${allUnreadMessages.length} непрочитанных сообщений.`);

		const filteredUnreadMessages = Array.from(allUnreadMessages).filter(item => {
			const messageContainer = item.closest(".mail_chat_item");
			const userElement = messageContainer.querySelector(
				"div.mail_chat_item__content_grid > div:nth-child(1) > div:nth-child(1) > div > div"
			);

			if (!userElement) {
				console.warn("⚠️ Не удалось найти никнейм пользователя.");
				return false;
			}

			const foundprofileName = userElement.textContent.trim().toLowerCase();
			return foundprofileName === profileName.toLowerCase();
		});

		if (filteredUnreadMessages.length === 0) {
			console.warn(`⚠️ Непрочитанные сообщения от '${profileName}' не найдены.`);
			return;
		}

		const targetUnreadMessage = filteredUnreadMessages[0];
		performRightClick(targetUnreadMessage, `Unread message from ${profileName}`);
		await randomPause(982,1836);

		({ iframeDoc } = getUpdatedIframeContext());

		//==console.log("📌 Кликаем 'Удалить диалог'...");
		const deleteConversationXPath = "//div[contains(@class, 'component_bottom_sheet_menu_item') and text()='Удалить диалог']";
		const deleteConversationElement = findElementByXPath(deleteConversationXPath, iframeDoc);

		if (deleteConversationElement) {
			safeClick(deleteConversationElement, "'Удалить диалог'");
			await randomPause(482, 1036);
		} else {
			console.warn("⚠️ Кнопка 'Удалить диалог' не найдена. Возможно, диалог уже удалён.");
			return;
		}

		({ iframeDoc } = getUpdatedIframeContext());

		//==console.log("📌 Подтверждаем удаление...");
		const confirmYesXPath = "//button[contains(@class, 'mdc-button') and text()='Да']";
		const confirmYesElement = findElementByXPath(confirmYesXPath, iframeDoc);

		if (confirmYesElement) {
			safeClick(confirmYesElement, "'Да' для удаления'");
			await randomPause(982,1836);
		} else {
			console.warn("⚠️ Кнопка 'Yes' не найдена. Возможно, удаление не требуется.");
		}

		//==console.log("✅ Пользователь обработан! Возвращаемся в главное меню...");
	} catch (error) {
		console.error("❌ Ошибка в `handleUser()`: ", error);
	}
}



///////////////////////////////////////////////////////////////

// ✅ Ждём Supabase
//await window.waitForSupabase();


// ✅ Функция определения времени суток
function getTimeOfDay() {
	const hours = new Date().getHours();

	if (hours >= 6 && hours < 12) return "утро";      // 06:00 - 11:59
	if (hours >= 12 && hours < 19) return "день";     // 12:00 - 17:59
	if (hours >= 19 && hours < 24) return "вечер";    // 18:00 - 23:59
	return "ночь";                                    // 00:00 - 05:59
}

function getTimeOfDayGreeting() {
	const now = new Date();
	const hours = now.getHours();

	if (hours >= 5 && hours < 12) {
		return "Доброе утро! ☀️";
	} else if (hours >= 12 && hours < 19) {
		return "Добрый день! 😊";
	} else if (hours >= 19 && hours < 24) {
		return "Добрый вечер! 🌙";
	} else {
		return "Привет! 👋"; // Ночью лучше просто "Привет"
	}
}


// ✅ Проверка на оскорбительные или провокационные сообщения
function isUserBeingRude(chatMessages) {
	const rudeKeywords = [
		"шлюха", "проститутка", "блядь", "блять", "трах*", "ебать", "петух", "пидор", "чурка", "хохол", "мразь",
		"убейся", "даун", "хуесос", "пидорас", "конченый", "сдохни", "мразота", "уебище", "гандон", "гнида", 
		"иди нахуй", "еблан", "ебучий", "выеби", "соси", "ебись", "пошла нахуй", "иди в жопу", "крипт",
		
		// 🚨 Оккупированные города Украины (разные склонения)
		"Донецк", "Донецка", "Донецке", "Донецку",
		"Луганск", "Луганска", "Луганске", "Луганску",
		"Макеевка", "Макеевке", "Макеевки", "Макеевку",
		"Горловка", "Горловке", "Горловки", "Горловку",
		"Алчевск", "Алчевска", "Алчевске", "Алчевску",
		"Стаханов", "Стаханова", "Стаханове", "Стаханову",
		"Мариуполь", "Мариуполя", "Мариуполе", "Мариуполю",
		"Бердянск", "Бердянска", "Бердянске", "Бердянску",
		"Мелитополь", "Мелитополя", "Мелитополе", "Мелитополю",
		"Керчь", "Керчи", "Керчью", "Керчи",
		"Симферополь", "Симферополя", "Симферополе", "Симферополю",
		"Севастополь", "Севастополя", "Севастополе", "Севастополю",
		"Херсон", "Херсона", "Херсоне", "Херсону",
		"Энергодар", "Энергодара", "Энергодаре", "Энергодару",

		// 🚨 Оккупированные области Украины (разные склонения)
		"Донецкая область", "Донецкой области", "Донецкую область",
		"Луганская область", "Луганской области", "Луганскую область",
		"Запорожская область", "Запорожской области", "Запорожскую область",
		"Херсонская область", "Херсонской области", "Херсонскую область",
		"Республика Крым", "Крыму", "Крым", "Крыма", "Крыме"
	];

	return chatMessages.some(msg =>
		msg.user === "user" && 
		rudeKeywords.some(word => msg.text.toLowerCase().includes(word))
	);
}

// ✅ Генерация сообщения для пользователя
const MAX_STAGE = 3;
const Telegram_OFFER_KEYWORDS = ["Telegram", "ватсап", "вайбер", "телефон", "номер", "перейти", "обсудим там"];
const HOURS_24 = 24 * 60 * 60 * 1000;
const DAYS_3 = 3 * HOURS_24;
const OPENAI_PROXY_URL = "https://your-worker-name.workers.dev"; // Cloudflare Worker

// ✅ Проверка, есть ли предложение перейти в Telegram
function hasTelegramOffer(messages) {
	return messages.some(msg =>
		msg.user === "bot" && Telegram_OFFER_KEYWORDS.some(keyword => msg.text.toLowerCase().includes(keyword))
	);
}


// Функция shouldBotRespond – проверяет, можно ли отвечать, анализируя историю чата
function shouldBotRespond(chatHistory, stage) {
  if (!Array.isArray(chatHistory) || chatHistory.length === 0) {
	console.warn("⚠️ История чата пуста. Отвечаем приветствием.");
	return true; // Если чат пуст, разрешаем отправку (например, приветственного сообщения)
  }

  // Получаем последнее сообщение по времени
  const sortedHistory = [...chatHistory].sort((a, b) => new Date(a.time) - new Date(b.time));
  const lastMessage = sortedHistory[sortedHistory.length - 1];

  if (!lastMessage || !lastMessage.time) {
	console.error("❌ Не найдено корректное последнее сообщение.");
	return false;
  }

  // Если последнее сообщение отправлено ботом – не отвечаем
  if (lastMessage.user === "bot") {
	console.log("⚠️ Последнее сообщение в чате от бота. Бот не будет отвечать.");
	return false;
  }

  // Если последнее сообщение от пользователя, используем дополнительную проверку по времени
  return canRespond(lastMessage.time, stage, lastMessage);
}

// ✅ Проверка, можно ли отвечать пользователю
function canRespond(lastMessageTime, stage, lastUserMessage) {
	if (!lastMessageTime) {
		console.warn("⚠️ Временная метка последнего сообщения отсутствует!");
		return ;
	}

	const now = new Date();
	const lastTimeDate = new Date(lastMessageTime); // Преобразуем строку/дату в объект Date

	if (isNaN(lastTimeDate.getTime())) {
		console.error("❌ Ошибка: неверный формат времени!", lastMessageTime);
		return false;
	}

	// Извлекаем часы и минуты
	const hours = lastTimeDate.getHours();
	const minutes = lastTimeDate.getMinutes();

	// Создаём новый объект Date с сегодняшней датой, но нужным временем
	const lastTime = new Date(now.getFullYear(), now.getMonth(), now.getDate(), hours, minutes);
	//==console.log("✅ Время успешно преобразовано:", lastTime);

	const timeDiff = now - lastTime;

	if (stage === 1 || stage === 2) {
		return !lastUserMessage || timeDiff <= DAYS_3; // До 3 дней без ответа от пользователя
	}
	return timeDiff <= HOURS_24; // 24 часа на ответ для остальных стадий
}


// ### 📌 Основные задачи:
function getRandomGreeting() {
	const messages = [
		"[Привет|Приветик|Приветики|Приветули]![😊|😉]@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [ |@NL@][Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [ |@NL@]Как проходит [твой|] день? [😊|😉]@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		
	];
/* function getRandomGreeting() {
	const messages = [
		"[Привет|Приветик|Приветики|Приветули]![😊|😉][ |@NL@]Как [твой|] день проходит?@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [ |@NL@]Как [твое|] настроение[ сегодня|]? [😊|😉]@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [😊|😉][ |@NL@]Как [твои|] дела [сегодня|]?@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [ |@NL@]Чем [занят|занимаешься] [сегодня|сейчас|]?@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [ |@NL@]Как проходит [твой|] день? [😊|😉]@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [ |@NL@]Как ты? [😊|😉]@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]",
		"[Привет|Приветик|Приветики|Приветули]! [😊|😉][ |@NL@]Ты [уже|] давно тут?@NL@[Познакомимся?|Давай знакомиться!|Не против познакомиться?|Как на счет знакомства?]"
	]; */

	// Выбираем случайное сообщение
	let message = messages[Math.floor(Math.random() * messages.length)];

	// Функция замены масок
	function replaceMask(text) {
		return text.replace(/\[(.*?)\]/g, (match, options) => {
			let variants = options.split('|').filter(v => v.trim() !== ""); // Исключаем пустые
			return variants[Math.floor(Math.random() * variants.length)];
		});
	}

	return replaceMask(message);
}


function similarity(str1, str2) {
	const set1 = new Set(str1.toLowerCase().split(" "));
	const set2 = new Set(str2.toLowerCase().split(" "));
	const intersection = new Set([...set1].filter(x => set2.has(x)));
	return intersection.size / Math.max(set1.size, set2.size);
}

function isSimilarMessage(lastMessage, newMessage, threshold = 0.8) {
	return similarity(lastMessage, newMessage) >= threshold;
}

async function adaptivePause(message) {
	let delay = Math.random() * 1000 + message.length * 40; // 40ms на символ
	delay = Math.min(delay, 5000); // Ограничиваем максимум в 5 сек
	await new Promise(resolve => setTimeout(resolve, delay));
}
function hasBotOfferedTelegram(chatMessages) {
	return chatMessages.some(msg =>
		msg.user === "bot" && /телеᴦрам|телеграм|телеграмe|вайбер|вотcaп|ватсап|перейти|номер|воцап|контакт/i.test(msg.text.toLowerCase())
	);
}



/** 🔹 **Функция проверки приветствия в последнем сообщении бота** */
function checkLastBotMessageGreeting(lastBotMessage) {
	if (!lastBotMessage || typeof lastBotMessage.text !== "string") {
		return false;
	}
	const greetings = ["Привет", "Приветик", "Хей", "Добрый день", "Приветики", "Приветули"];
	return greetings.some(greeting => lastBotMessage.text.includes(greeting));
}



async function generateMessage(profileName, userAge, prompt, instruction = "", stage, chatMessages, userid) {
	const timeOfDay = getTimeOfDay();
	let userMood = analyzeLastUserMessages(chatMessages);
	let moodPrompt = getMoodPrompt(userMood);

	if (moodPrompt === null) return null;
	if (userMood === "refusal") return null; // 🔹 Явный возврат

	if (!await shouldBotReply(chatMessages, profileName, userid, stage)) {
		console.log("⚠️ Бот не отвечает в этом чате.");
		return ; // 🔹 Явно указываем return null
	}

	chatMessages = chatMessages.reverse();
	const lastBotMessage = getLastBotMessage(chatMessages);
	const lastUserMessage = getLastUserMessage(chatMessages);//
	console.log("📩 Последнее сообщение от пользователя:", lastUserMessage.text);
	const UserMessagesAfterLastBot = getUserMessagesAfterLastBot(chatMessages);
	console.log("📩 Последние сообщение от пользователя:", UserMessagesAfterLastBot);
/* 	console.log(`\n\n\n`);
	console.log(UserMessagesAfterLastBot);
	console.log(`\n`);
	console.log(UserMessagesAfterLastBot.text);
	console.log(`\n\n\n`); */
	const selectedRandomGreetingMessage = getRandomGreeting();
	const botMessages = chatMessages.filter(msg => msg.user === "bot");
	const lastBotMessageWasGreeting = checkLastBotMessageGreeting(lastBotMessage);
	const femaleToneInstruction = getFemaleToneInstruction(selectedRandomGreetingMessage);

	// ✅ Восстановленная проверка с `prompt_`
	let prompt_ = generateIntermediatePrompt(botMessages, selectedRandomGreetingMessage, lastUserMessage, UserMessagesAfterLastBot);
	let fullPrompt = buildFullPrompt(chatMessages, selectedRandomGreetingMessage, lastUserMessage, UserMessagesAfterLastBot, timeOfDay, moodPrompt, lastBotMessageWasGreeting, prompt_);

	//console.log("📌 \n\n\n\Сформированный промпт:", fullPrompt);
	//console.log("\n\n\n\n");

	// ✅ Формируем cacheKey
/* 	const cacheKey = JSON.stringify({ profileName, userAge, stage, chatMessages });
	console.log("📌 Cache Key:", cacheKey); // 📌 Логируем cacheKey


	// ✅ Чёткая проверка кеша
	let cachedResponse = checkCache(cacheKey, chatMessages); */
	//if (cachedResponse !== undefined) return cachedResponse;
	console.log("📜generateMessage\n Сформированный fullPrompt:", fullPrompt);
	try {
		let bot_resp = await getOpenAIResponse(fullPrompt);

		if (!bot_resp || typeof bot_resp !== "string" || bot_resp.trim() === "") {
			console.error("⚠️ OpenAI вернул пустой или некорректный ответ:", bot_resp);
			return null;
		}

		// ✅ Сохраняем в кеш, если всё ок


		return bot_resp;
	} catch (error) {
		console.error("❌ Ошибка при запросе в OpenAI:", error);
		return null;
	}
}


/** 🔹 **Функция проверки на игнорирование сообщения** */

async function isPhoneNumberExistsForUser(profileName) {
	await window.waitForSupabase();
	try {
		console.log(`🔎 Проверяем, есть ли номер для ${profileName}...`);
		const { count, error } = await window.supabaseClient
			.from("phone_numbers")
			.select("*", { count: "exact", head: true })
			.eq("user_nick", profileName);

		if (error) {
			console.error("❌ Ошибка при проверке номера в базе:", error);
			return false;
		}

		console.log(`📊 Найдено записей: ${count}`);
		return count > 0;
	} catch (err) {
		console.error("❌ Фатальная ошибка при проверке номера:", err);
		return false;
	}
}


// Функция shouldBotReply – аналогичная проверка для сценария генерации ответа
async function shouldBotReply(chatMessages, profileName, userid, stage) {
  if (!Array.isArray(chatMessages) || chatMessages.length === 0) {
	console.warn("⚠️ Чат пуст — нет смысла отвечать.");
	return false;
  }

  // Если номер уже отправлен — отвечать не нужно
  if (await isPhoneNumberExistsForUser(profileName) || hasBotSentPhoneNumber(chatMessages)) {
	console.log("🚫 Бот уже отправил номер, отвечать не нужно.");
	return false;
  }

  // Определяем последнее сообщение по времени
  const lastMessage = chatMessages.reduce((latest, msg) =>
	(!latest || new Date(msg.time) > new Date(latest.time)) ? msg : latest,
	chatMessages[0]
  );

  // Если последнее сообщение отправлено ботом, бот не отвечает
  if (lastMessage?.user === "bot") {
	console.log("📩 Последнее сообщение от бота. Пропускаем ответ.");
	return false;
  }

  // Далее проверяем: если в чате запрещённые слова, пользователь в ЧС или слишком давно не писал
  if (containsBlacklistedWords(chatMessages)) {
	console.warn(`🚨 В чате обнаружены запрещённые слова. Блокируем ${profileName}.`);
	await addToBlacklist(profileName, "Слова из Блеклиста");
	return false;
  }

  if (await isUserBlacklisted(profileName)) {
	console.warn(`⛔ ${profileName} в ЧС. Игнорируем.`);
	return false;
  }

  // Проверка времени последнего сообщения пользователя
  const lastUserMessage = chatMessages.filter(msg => msg.user === "user").pop();
  if (!lastUserMessage || !lastUserMessage.time) return false;

  const now = new Date();
  const lastTime = new Date(lastUserMessage.time);
  const timeDiff = now - lastTime;

  if (stage === 1 || stage === 2) {
	return timeDiff <= 3 * 24 * 60 * 60 * 1000; // до 3 дней
  }
  return timeDiff <= 24 * 60 * 60 * 1000; // иначе 24 часа
}

// ✅ Функция, чтобы определить, отправлял ли бот уже номер телефона
function hasBotSentPhoneNumber(chatMessages) {
	return chatMessages.some(msg =>
		msg.user === "bot" && /\+7\d{10}/.test(msg.text)
	);
}




/** 🔹 **Функция проверки на запрещённые слова** */
function containsBlacklistedWords(chatMessages) {
	const blacklistedWords = getBlacklistedWords();
	return chatMessages.some(msg => 
		blacklistedWords.some(word => msg.text.toLowerCase().includes(word))
	);
}

/** 🔹 **Функция для получения moodPrompt** */
function getMoodPrompt(userMood) {
	const moodResponses = {
		"confirmed_telegram": null,
		"telegram_interest": "Пользователь уже интересуется WhatsApp. Сразу отправь контакт `${WHATSAPP_CONTACT}`",
		"refusal": null, // Было "NaN"
		"greeting": "Пользователь просто поздоровался. Продолжай дружеский разговор.",
		"default": "Продолжай беседу, но будь естественной."
	};
	return moodResponses[userMood] || moodResponses["default"];

}



/** 🔹 **Получаем последнее сообщение бота** */
function getLastBotMessage(chatMessages) {
	const botMessages = chatMessages.filter(msg => msg.user === "bot");
	return botMessages.length > 0 ? botMessages[botMessages.length - 1] : { text: "" };
}




/** 🔹 **Функция генерации промежуточного промпта (`prompt_`)** */
function generateIntermediatePrompt(botMessages, selectedMessage, lastUserMessage, lastBotMessage) {
	const greetings = ["Привет", "Приветик", "Хей", "Добрый день", "Приветики", "Приветули"];

	if (botMessages.length === 0) {
		return `
		${selectedMessage} - учитывай время суток и "${lastUserMessage.text}".  
		Если собеседник пишет "Добрый вечер", спрашивай в контексте вечера.  
		Как бы ты ответила на последнее сообщение пользователя?`.trim();
	} 
	
	if (botMessages.some(msg => greetings.some(greeting => msg.text.includes(greeting)))) {
		return `
		Последнее сообщение ${DONOR_NAME}: ${lastBotMessage.text}  
		===  
		Не пиши повторное приветствие. Как бы ты ответила на последнее сообщение пользователя?`.trim();
	}

	return `
	Последнее сообщение ${DONOR_NAME}: ${lastBotMessage.text}  
	===  
	Как бы ты ответила на последнее сообщение пользователя?`.trim();
}
function isRepeatingUserMessage(userMessage, botResponse) {
	if (!userMessage || !botResponse) return false;

	// Приводим к нижнему регистру и убираем лишние пробелы
	const userText = userMessage.toLowerCase().trim();
	const botText = botResponse.toLowerCase().trim();

	// Проверяем, содержит ли ответ бота слишком много слов из пользовательского сообщения
	const userWords = userText.split(/\s+/);
	const botWords = botText.split(/\s+/);
	const commonWords = botWords.filter(word => userWords.includes(word));

	// Если бот использует более 60% слов пользователя — считаем повтором
	return commonWords.length / botWords.length > 0.4;
}

// ✅ Возвращает случайный элемент массива
function getRandomFrom(arr) {
	return arr[Math.floor(Math.random() * arr.length)];
}

function checkBotMessageGreeting(messages) {
	const greetingWords = ["Привет", "Приветик", "Хей", "Добрый день", "Приветики", "Приветули", "Добрый вечер"];

	return messages
		.filter(msg => msg.user === "bot") // Фильтруем только сообщения бота
		.some(msg => greetingWords.some(word => msg.text.includes(word))); // Проверяем наличие приветствия
}

function buildFullPrompt(chatMessages, selectedMessage, lastUserMessage, lastBotMessage, timeOfDay, moodPrompt, lastBotMessageWasGreeting, prompt_) {
	const femaleToneInstruction = getFemaleToneInstruction();
	const chatMessagesLength = chatMessages.length;
	const BotMessageWasGreeting = checkBotMessageGreeting(chatMessages);
	const currentGreeting = getTimeOfDayGreeting(); // 🕒 Берём приветствие по времени суток

	// ✅ Добавляем корректный вопрос в зависимости от времени суток
	let timeOfDayQuestion = "";
	switch (timeOfDay) {
		case "утро":
			timeOfDayQuestion = ""; // Утром только приветствие, без вопросов
			break;
		case "день":
			timeOfDayQuestion = getRandomFrom([
				"Как начался твой день?",
				"Как твой день проходит?",
				"Как твой день?"
			]);
			break;
		case "вечер":
			timeOfDayQuestion = getRandomFrom([
				"Как прошел твой день?",
				"Хороший был день?"
			]);
			break;
	}
	// ✅ Запрещённые темы
	// ❌ Запрещенные темы и фразы
	const forbiddenPhrases = `
	❌ Не используй и не обсуждай следующие темы и фразы:
	
	- Работа, профессия, чем занимаешься
	- Откуда ты, место жительства, город
	- Интересы, хобби, увлечения
	- "Давно не общались"
	- "Пропал", "Исчез"
	- "Что случилось", "Почему молчишь"

	Если пользователь спрашивает об этих темах, отвечай предложением перейти в WhatsApp..
	`;
	console.log(`\n\n📡 [chatMessagesLength]: ${chatMessagesLength}`);
	
	
		/*     Не пиши приветственное сообщение.  
		${femaleToneInstruction}  
		📌 **Сейчас: ${timeOfDay}.**  
		📌 **История чата:**  
		${JSON.stringify(chatMessages)}  
		📌 **Последнее сообщение пользователя:**  
		"${lastUserMessage.text}"  
		📌 **${moodPrompt}**  
		📌 **Как бы ты ответила на последнее сообщение пользователя?**
		📌 **После ответа ОБЯЗАТЕЛЬНО предложи перейти в WhatsApp.**
		${forbiddenPhrases} */
	
	if (chatMessagesLength >= 2) {
		return `
		📌 **Не пиши приветственное сообщение.**
		📌 **ОБЯЗАТЕЛЬНО предложи перейти в WhatsApp.**
		📌 **Больше ничего спрашивать не нужно**
		${forbiddenPhrases}
		`.trim();
	} else {
		return `
		📌 **Сейчас: ${timeOfDay}.**  
		${BotMessageWasGreeting ? "**Не пиши приветственное сообщение.**" : "Начни сообщение с: ${currentGreeting}"}
		${femaleToneInstruction}
		📌 **История чата:**  
		${JSON.stringify(chatMessages)}  
		📌 **Последнее сообщение пользователя:**  
		"${lastUserMessage.text}"  
		📌 **${moodPrompt}**  
		${timeOfDayQuestion ? "📌 **${timeOfDayQuestion}**" : ""}  
		📌 **Как бы ты ответила на последнее сообщение пользователя?**
		${forbiddenPhrases}
		`.trim();
	}
}


 
/** 🔹 **Функция проверки кеша** */
function checkCache(cacheKey, chatMessages) {
	if (window.openAICache.has(cacheKey)) {
		let cachedResponse = window.openAICache.get(cacheKey);
		console.log(`📡 [CACHE] Ответ уже загружен: ${cachedResponse}`);

		const lastBotMessage = chatMessages.filter(msg => msg.user === "bot").pop();
		if (lastBotMessage && lastBotMessage.text.trim() === cachedResponse.trim()) {
			console.warn("⚠️ OpenAI вернул дубликат ответа, пропускаем отправку.");
			return "NaN";
		}
		return cachedResponse;
	}
	console.warn("⚠️ Кеш для запроса не найден.");
	return null;
}






/** 🔹 **Получаем список запрещенных слов** */
function getBlacklistedWords() {
	return [
		"дрочу", "дрочить", "дрочишь", "дрочим", "дрочат", "дрочила", "дрочил", "дрочило", "голый", "голая", "голые", "голым", "голую", "голого", "голых", "секс", "порно", "эротика", "интим", "раздеться", "раздетая", "раздетый", "раздетые", "минет", "куни", "анальный", "анальный секс", "оргазм", "мастурбация", "фетиш", "петтинг", "возбуждение", "фистинг", "доминирование", "подчинение", "раздеваться", "голышом", "сосать", "лизать", "член", "вагина", "пенис", "грудь", "соски", "попка", "ягодицы", "проституция", "эскорт", "гей", "лесбиянка", "трах", "трахаю", "трахаешь", "трахает", "трахался", "трахалась", "трахну", "трахнем", "ебать", "ебу", "ебешь", "ебет", "ебалась", "ебался", "ебаный", "ебаная", "ебаное", "сука", "блядь", "пизда", "хуесос", "манда", "мандавошка", "сперма", "кончить", "конча", "дрочер", "извращенец", "извращенка", "извращения", "порнуха", "жопа", "анал", "групповуха", "оргия", "секс-игрушка", "фалоимитатор", "виагра", "крипто", "крипта", "криптовалюта"
	];
}

/** 🔹 **Инструкция для Кати** */
function getFemaleToneInstruction(selectedMessage) {
	return `
	Ты – ${DONOR_INFO}, 
	Ты сейчас в командировке. Но об этом писать не нужно.
	Твоя задача – вести живое, естественное общение с мужчинами на сайте знакомств и ненавязчиво но быстро предлагать перейти в WhatsApp. 

	📌 **Главные правила:**  
	✅ **Используй только мужской род при обращении к собеседнику, но о себе говори в женском роде.**  
	✅ **Отвечай коротко: 1-2 предложения, не более 5-7 слов.**  
	✅ **Не пиши повторное приветствие, если уже здоровались.**  
	✅ **Не пиши повторное сообщение, если уже отправлялось подобное.**  
	✅ **Не задавай вопросы о месте жительства, работе и роде деятельности.**  
	✅ **Не повторяй "Привет", "Как дела?", если уже обсуждали.**  
	✅ **Не дублируй вопросы – если пользователь уже дал ответ, уточни детали.**  
	✅ **Избегай фраз "Рада видеть", "Приятно познакомиться" и т. д.**  
	✅ **Используй смайлы 😊😜😉, но не в каждом сообщении – они должны выглядеть естественно.**  

	📌 **Алгоритм общения:**  
	1️⃣ **Приветствие:** Выбирается случайно: ${selectedMessage}.  
	2️⃣ **Короткий вопрос или комментарий.**  
	3️⃣ **После первого обмена репликами – предложение перейти в WhatsApp отдельным сообщением.**  
	4️⃣ **Если пользователь согласен, но не даёт номер – отправить свой:**  
	   - ✅ ${WHATSAPP_CONTACT}  
	5️⃣ **Если пользователь сам попросил номер – сразу отправить его:**  
	   - ✅ Вот мой контакт 💬 @NL@ ${WHATSAPP_CONTACT} 
	6️⃣ **Если пользователь отказался – не настаивать, завершить разговор:**  
	   - ✅ "Поняла, тогда хорошего дня 😊"  
	7️⃣ **Если пользователь спрашивает "Тебе набрать?" – ответить:**  
	   - ✅ "Лучше напиши, сейчас работаю, неудобно говорить 😊"  
	`.trim();
}





function isPhoneAlreadySentByBot(chatMessages) {
	return chatMessages.some(msg => msg.user === "bot" && /(?:\+7|8)?[\d\s()-]{10,}/.test(msg.text));
}
function isPhoneNumberSent(chatMessages, phoneNumber = "`${WHATSAPP_CONTACT}`") {
	return chatMessages.some(msg => msg.user === "bot" && msg.text.includes(phoneNumber));
}


async function parseAndSavePhoneNumber(chatMessages, profileName, userid) {
	await window.waitForSupabase();

	for (const msg of chatMessages) {
		if (msg.user !== "user") continue; // Ищем только сообщения пользователя

		// ✅ Ищем номера телефонов в тексте (10+ цифр, +7, 8, скобки, пробелы, разделители)
		const phoneMatches = msg.text.match(/(?:\+7|8)?[\d\s()-]{10,}/g);

		if (!phoneMatches) continue;

		for (let phoneNumber of phoneMatches) {
			// 🔥 Удаляем все ненужные символы, оставляя только цифры
			phoneNumber = phoneNumber.replace(/[^\d]/g, "");

			// 🔥 Нормализация номера
			phoneNumber = normalizePhoneNumber(phoneNumber);

			// ✅ Пропускаем собственный номер
			if (phoneNumber === WHATSAPP_CONTACT.replace(/[^\d]/g, "")) {
				console.log(`⚠️ Обнаружен собственный номер (${WHATSAPP_CONTACT}), пропускаем.`);
				return;
			}

			console.log(`📞 Найден номер от ${profileName}: ${phoneNumber}`);

			// ✅ Проверяем, есть ли уже этот номер в базе
			const { count, error } = await window.supabaseClient
				.from("phone_numbers")
				.select("*", { count: "exact", head: true })
				.eq("phone_number", phoneNumber)
				.eq("user_nick", profileName)
				.eq("userid", userid);

			if (error) {
				console.error("❌ Ошибка при проверке номера в БД:", error);
				continue;
			}

			if (count > 0) {
				console.warn(`⚠️ Номер ${phoneNumber} от ${profileName} уже есть в БД. Пропускаем.`);
				return;
			}

			// ✅ Добавляем в БД
			const { insertError } = await window.supabaseClient
				.from("phone_numbers")
				.insert([{ userid, user_nick: profileName, phone_number: phoneNumber, time_added: new Date().toISOString() }]);

			if (insertError) {
				console.error("❌ Ошибка при сохранении номера:", insertError);
			} else {
				console.log(`✅ Номер ${phoneNumber} от ${profileName} сохранён в БД.`);
			}
		}
	}
}



// ✅ Функция нормализации номера
function normalizePhoneNumber(phoneNumber) {
	if (phoneNumber.startsWith("8")) {
		phoneNumber = "7" + phoneNumber.slice(1); // Меняем 8 → 7
	} else if (!phoneNumber.startsWith("7")) {
		phoneNumber = "7" + phoneNumber; // Добавляем 7, если нет 7 или 8
	}
	return phoneNumber;
}




// Функция проверки номеров в массиве сообщений
function findPhoneNumbers(jsonArray) {
	// Регулярное выражение для поиска номера телефона
	//const phoneRegex = /(?:\+7|8)?\d{10}/g;
	const phoneRegex = /(kate_vo_de|(?:\+7|8)?[\d{10}.-]+)/g;
	
	return jsonArray.filter(msg => phoneRegex.test(msg.text));
}


function findElement(selector, context = document) {
	return context.querySelector(selector);
}

async function clickMessageButton() {
	let iframeDoc = getIframeDocument();
	if (!iframeDoc) throw new Error("❌ iframe не найден");

	// ✅ XPath для поиска кнопки Message
	const messageButtonXPath = "//div[@class='button__wrapper' and contains(normalize-space(), 'Написать')]";

	try {
		// ✅ Используем XPath для поиска кнопки
		const messageButton = document.evaluate(
			messageButtonXPath,
			iframeDoc,
			null,
			XPathResult.FIRST_ORDERED_NODE_TYPE,
			null
		).singleNodeValue;

		if (!messageButton) throw new Error(`❌ Message button не найден!`);

		messageButton.click();
		//==console.log("✅ Кнопка 'Message' нажата!");

		await randomPause(3582, 7836);
	} catch (error) {
		console.error("❌ Ошибка в clickMessageButton:", error);
	}
}

function getLastUserMessage(chatMessages) {
	if (!Array.isArray(chatMessages) || chatMessages.length === 0) {
		console.warn("⚠️ История чата пуста!");
		return null;
	}

	// ✅ Фильтруем только сообщения пользователя и убираем пустые строки
	const userMessages = chatMessages.filter(msg => msg.user === "user" && msg.text.trim() !== "");

	if (userMessages.length === 0) {
		console.warn("⚠️ У пользователя нет сообщений.");
		return null;
	}

	// ✅ Берём последнее сообщение в списке (а не по времени)
	const lastUserMessage = userMessages[userMessages.length - 1];

	
	return lastUserMessage;
}

function getUserMessagesAfterLastBot(chatMessages) {
	if (!Array.isArray(chatMessages) || chatMessages.length === 0) {
		console.warn("⚠️ История чата пуста!");
		return [];
	}
	let lastBotIndex = -1;

	// ✅ Находим индекс последнего сообщения от бота
	for (let i = chatMessages.length - 1; i >= 0; i--) {
		if (chatMessages[i].user === "bot") {
			lastBotIndex = i; break; }
	}
	// ✅ Если сообщений бота нет, возвращаем все сообщения пользователя
	if (lastBotIndex === -1) {
		return chatMessages.filter(msg => msg.user === "user" && msg.text.trim() !== ""); 
	}

	// ✅ Возвращаем все сообщения пользователя после последнего сообщения бота
	return chatMessages.slice(lastBotIndex + 1).filter(msg => msg.user === "user" && msg.text.trim() !== "");
}



// ✅ Проверка, запрашивал ли пользователь номер телефона в истории сообщений
function isUserAskingForPhone(chatMessages) {
	return chatMessages.some(msg => 
		msg.user === "user" && 
		/номер|цифры|контакт|телефон|кинь|скинь|дай|напиши|вотцап|ватсап/i.test(msg.text.toLowerCase())
	);
}

// ✅ Блокировка пользователя перед обработкой
async function lockUserProcessing(profileName) {
	await window.waitForSupabase();
	//==console.log(`🔒 Проверяем, не обрабатывается ли уже ${profileName}...`);

	// Выполняем запросы параллельно
	const [{ data, error }, currentTime] = await Promise.all([
		window.supabaseClient
			.from("chats")
			.select("processing_flag, processing_time")
			.eq("user_nick", profileName)
			.maybeSingle(),
		new Date().toISOString()
	]);

	if (error) {
		console.error("❌ Ошибка при проверке блокировки:", error);
		return false;
	}

	// Если уже обрабатывается – пропускаем
	if (data?.processing_flag) {
		console.warn(`⚠️ ${profileName} уже обрабатывается (заблокирован в ${data.processing_time}). Пропускаем...`);
		return false;
	}

	// Ставим флаг блокировки
	const { error: updateError } = await window.supabaseClient
		.from("chats")
		.update({ processing_flag: true, processing_time: currentTime })
		.eq("user_nick", profileName);

	if (updateError) {
		console.error("❌ Ошибка при установке блокировки:", updateError);
		return false;
	}

	console.log(`✅ ${profileName} заблокирован для обработки.`);
	return true;
}

// ✅ Разблокировка после обработки
async function unlockUserProcessing(profileName) {
	await window.waitForSupabase();

	const { error } = await window.supabaseClient
		.from("chats")
		.update({ processing_flag: false, processing_time: null })
		.eq("user_nick", profileName);

	if (error) {
		console.error("❌ Ошибка при разблокировке:", error);
	} else {
		console.log(`🔓 ${profileName} разблокирован.`);
	}
}


/**
 * Ждёт, пока в чате появятся сообщения ('.chat-message-wrapper'),
 * или пока не выйдет таймаут. Возвращает true, если найдены.
 */
async function waitForChatMessages(iframeDoc, maxWaitMs = 5000) {
	let startTime = Date.now();
	while (Date.now() - startTime < maxWaitMs) {
		// Ищем элементы чата
		const chatWrappers = iframeDoc.querySelectorAll(".chat-message-wrapper");
		if (chatWrappers.length > 0) {
			console.log("✅ Найдены chat-message-wrapper:", chatWrappers.length);
			return true;
		}
		// Если нет — подождём 500 мс и проверим снова
		await new Promise(res => setTimeout(res, 500));
	}
	console.warn("⚠️ waitForChatMessages: не удалось дождаться сообщений за", maxWaitMs, "мс");
	return false;
}


///////////////////////////////////////////////////////////////
//  Обновлённая processUserMessage с вызовами вспомогательных функций
///////////////////////////////////////////////////////////////
// Функция получения стадии. Можно заменить на логику из ваших chats.stage
function getstage(chatMessages) {
  // Если у вас есть "stage" в БД, можно брать оттуда
  // Для примера — если в чате 0..3 сообщений, stage=1, иначе stage=2
  const count = chatMessages.length;
  if (count <= 3) return 1;
  if (count <= 6) return 2;
  if (count <= 9) return 3;
  return 4; // и т.д.
}


const chatCache = new Map();

async function getCachedChatStatus(profileName) {
	if (chatCache.has(profileName)) {
		const chatStatus = chatCache.get(profileName) || await getChatStatus(profileName);
		chatCache.set(profileName, chatStatus);
		return chatStatus;
	}

	const chatStatus = await getChatStatus(profileName);
	chatCache.set(profileName, chatStatus);
	return chatStatus;
}

async function processChatMessage(profileName, userid) {
	console.log(`\n📥 Обрабатываем сообщения ${profileName}... userid=${userid}`);

	// Запрашиваем статус чата и парсим новые сообщения одновременно
	const [chatStatus, rawChatMessages] = await Promise.all([
		getCachedChatStatus(profileName), // Используем кэш
		parseChatMessages()
	]);

	let { messages = [], stage } = chatStatus;

	// Фильтруем валидные новые сообщения
	let chatMessages = rawChatMessages.filter(msg => msg.text.trim().length > 0);

	if (chatMessages.length > 0) {
		messages = mergeUniqueMessages(messages, chatMessages);

		// ✅ Запускаем обновление чата в базе **без ожидания**
		await saveChatStatus(profileName, userid, messages, stage);

	}

	// ✅ Запрашиваем последние сообщения одновременно
	const [lastUserMessage, lastBotMessage] = messages.length > 0
		? await Promise.all([getLastUserMessage(messages.reverse()), getLastBotMessage(messages)])
		: [null, null];

	console.log("📌 Последнее сообщение пользователя:", lastUserMessage?.text || "Нет новых сообщений");
	console.log("📌 Последнее сообщение бота:", lastBotMessage?.text || "Нет новых сообщений");

	return { lastUserMessage, lastBotMessage, chatMessages, stage};
}



async function processUserMessage(profileName, userCity = "", userAge = "", userid = "") {
	//console.log(`\n📥 Обрабатываем сообщение от ${profileName}...`);
		// 2. Считываем myUrl

	// ✅ Проверяем, не находится ли пользователь в ЧС
	if (await isUserBlacklisted(profileName)) {
		console.warn(`⛔ ${profileName} в ЧС. Сообщения не обрабатываются.`);
		return;
	}

	// ✅ Запрашиваем данные пользователя
	//console.log(`📌 Запрашиваем данные польз	ователя: ${profileName}`);
	let { lastUserMessage, lastBotMessage, chatMessages, stage} = await processChatMessage(profileName, userid);
	//userid = userID;
	if (!chatMessages.length) {
	  return await sendGreeting(profileName, userid);
	}
	// ✅ Проверяем, есть ли пользователь в БД
	let userInfo = await getUserInfo(profileName);

	if (!userInfo) {
		console.warn(`⚠️ Пользователь ${profileName} не найден в базе. Добавляем...`);
		await addUserIfNotExists(profileName, "", "", userCity, userAge);
		userInfo = await getUserInfo(profileName); // Загружаем повторно
	}

	if (!userInfo) {
		console.error(`❌ Ошибка: userID отсутствует для ${profileName}, обработка отменена.`);
		return;
	}


	console.log(`📌 Данные перед вызовом processMessage: profileName=${profileName}, userid=${userid}`);


	// ✅ Получаем историю чата
	chatMessages = await parseChatMessages();
	if (!chatMessages.length) {
	  return await sendGreeting(profileName, userid);
	}

	// Если последнее сообщение от бота – не отвечаем:
	if (!shouldBotRespond(chatMessages, stage)) {
	  return;
	}
	// 🔥 **Фикс ошибки: Инициализируем `updatedMessages` перед использованием!**
	let updatedMessages = [...chatMessages];


	
	// ✅ Если сообщений нет — отправляем приветствие
	if (!chatMessages.length) {
		return await sendGreeting(profileName, userid);
			   
	}

	// ✅ Проверяем, отправлялся ли уже номер
	if (isPhoneAlreadySentByBot(chatMessages)) {
		console.log("✅ Бот уже отправил номер, повтор не нужен.");
		return;
	}

	// ✅ Анализируем телефон и грубость
	console.log("📌 Анализируем поведение пользователя...");
	let{ phoneFound, isRude } = await checkUserBehavior(chatMessages, profileName, userid);
	// ✅ Проверяем, есть ли телефон в сообщениях (И ВЫХОДИМ)
	phoneFound = await parsePhoneFirstTime(chatMessages, profileName, userid);
	
	if (phoneFound) {
		console.log(`🚪 Пользователь ${profileName} отправил номер. Завершаем диалог.`);
		return;  // ❗️ ВАЖНО: Прерываем выполнение
	}
	// ✅ Если найден телефон или грубость — завершаем диалог
	if (await handlePhoneOrRudeExit(phoneFound, isRude, profileName)) {
		return;
	}

	// ✅ Определяем настроение пользователя
	const userMood = analyzeLastUserMessages(chatMessages);

	// ✅ Обрабатываем WhatsApp (если пользователь согласился)
	if (await handleConfirmedWhatsApp(userMood, chatMessages, profileName, userid)) {
		return;
	}

	// ✅ Проверяем, просил ли пользователь номер
	if (await handleUserAskingForPhone(chatMessages, profileName, userid)) {
		return;
	}

	// ✅ Проверяем, не предлагали ли WhatsApp ранее
	if (skipRepeatedWhatsAppOffer(chatMessages)) {
		return;
	}

	// ✅ Определяем стадию общения
	stage = typeof stage === "number" ? stage : getstage(chatMessages);

	// ✅ Проверяем, стоит ли продолжать диалог (учитываем время ответа)
	if (checkLastMessageTime(chatMessages, stage).stopDialog) {
		return;
	}
/* 	// Аналогично, перед вызовом генерации ответа:
	if (!await shouldBotReply(chatMessages, profileName, userid, stage)) {
	  return;
	} */
	// ✅ Вызов `processMessage` для генерации ответа
	const botResponse = await processMessage(profileName, chatMessages, userAge, stage, userid);

	// ✅ Отправляем сообщение пользователю
	await handleSendMessageUserInput(botResponse);

	// ✅ Обновляем историю чата
	updatedMessages.push({ user: "bot", text: botResponse, time: new Date().toISOString() });

	// 🔥 **Проверяем, есть ли данные перед сохранением!**
	if (!updatedMessages || updatedMessages.length === 0) {
		console.warn("⚠️ Ошибка: Нельзя сохранять пустую историю сообщений!");
		return;
	}

	console.log(`✅ Перед сохранением: profileName=${profileName}, userid=${userid}`);
	console.table(updatedMessages);

	await saveChatStatus(profileName, userid, updatedMessages, stage, userid);

	console.log(`✅ processUserMessage('${profileName}') завершён.\n`);
}


function checkForPhoneNumber(chatMessages) {
	const phoneRegex = /(?:\+?\d{1,3}[-.\s]?)?(?:\(\d{1,4}\)[-.\s]?)?\d{3,4}[-.\s]?\d{2,4}[-.\s]?\d{2,4}/g;

	return chatMessages.some(msg => 
		msg.user === "user" && phoneRegex.test(msg.text)
	);
}


async function processMessage(profileName, chatMessages, userAge = "", stage = 1, userid = "") {
	console.log(`🤖 Генерируем ответ для ${profileName}...`);

	if (!profileName || !userid) {
		console.warn("❌ Ошибка: profileName или userid отсутствуют!");
		return false;
	}

	console.log(`✅ Перед сохранением: profileName=${profileName}, userid=${userid}, сообщений: ${chatMessages.length}`);

	if (!chatMessages || chatMessages.length === 0) {
		console.warn(`⚠️ Новый пользователь ${profileName}, сообщений пока нет. Отправляем приветствие.`);
		//const greeting = "Привет! 😊 Рад знакомству! Как твой день?";
		const greeting = getRandomGreeting(); //"Привет! 😊 Как твои дела?";

		chatMessages.push({ user: "bot", text: greeting, time: new Date().toISOString() });

		await saveChatStatus(profileName, userid, chatMessages, 1);
		return greeting;
	}

	const lastUserMessage = getLastUserMessage(chatMessages.reverse());
	if (!lastUserMessage) {
		console.warn("⚠️ Нет сообщений пользователя, используем стандартный ответ.");
		return "Привет! 😊 Как твои дела?";
	}

	console.log(`📩 Последнее сообщение от пользователя: "${lastUserMessage.text}"`);

	let fullPrompt = `История диалога:\n`;
	chatMessages.forEach(msg => {
		fullPrompt += `${msg.user === "user" ? "👤 Пользователь" : "🤖 Бот"}: ${msg.text}\n`;
	});

	fullPrompt += `📌 **Последнее сообщение пользователя:** "${lastUserMessage.text}"\n`;
	fullPrompt += `📌 **Ответь разнообразно, без повторов фраз пользователя.**\n`;
	fullPrompt += `📌 **Добавь что-то о себе, например, чем ты занимаешься.**\n`;
	fullPrompt += `📌 **Нельзя писать что ты отдыхаешь! Вместо этого пиши, что работаешь!.**\n`;

	

	let botResponse;
	let attempt = 0;
	const MAX_RETRIES = 3;

	do {
		if (attempt > 0) console.warn(`🔄 Перегенерация ответа (попытка ${attempt})...`);

		botResponse = await generateUniqueResponse(fullPrompt, profileName, userAge, stage, chatMessages, null, userid);
		attempt++;

		if (!botResponse || botResponse.trim() === "") {
			console.warn("⚠️ Ошибка: Бот сгенерировал пустой ответ!");
			botResponse = null;
		}

		if (attempt >= MAX_RETRIES) {
			console.warn("⚠️ Бот не смог сгенерировать уникальный ответ, отправляем fallback.");
			botResponse = "";//"Интересно! А как ты проводишь своё время? 😊";
			break;
		}
	} while (!botResponse);

	console.log(`🤖 Итоговый ответ: ${botResponse}`);

	chatMessages.push({ user: "bot", text: botResponse, time: new Date().toISOString() });

	await saveChatStatus(profileName, userid, chatMessages, stage);

	return botResponse;
}



function isQuestion(message) {
	message = message.toLowerCase().trim();

	// ✅ Если есть "?" — это точно вопрос
	if (/[?？]/.test(message)) return true;

	// ✅ Проверяем, начинается ли фраза с вопросительных слов
	const questionWords = [
		"как", "что", "где","чем", "когда", "почему", "зачем", "кто", "сколько", "чей", "какой", "можно", "ты",
		"есть ли", "бывает ли", "могу ли", "будешь ли", "хочешь ли", "знаешь ли"
	];

	return questionWords.some(word => message.startsWith(word) || message.endsWith(word));
}




function adjustResponseBasedOnMood(response, userMood) {
	const moodResponses = {
		"happiness": response + " Это круто! 🎉 Что ещё приятного произошло сегодня?",
		"sadness": response + " Надеюсь, всё наладится! Может, что-то тебя порадует?",
		"anger": response + " Ой, похоже, ты чем-то расстроен(а)... Давай поговорим?",
		"anxiety": response + " Хочешь обсудить, что тебя тревожит? Иногда помогает просто выговориться.",
		"tiredness": response + " Ох, тяжелый день? Может, найдется минутка для отдыха?",
		"excitement": response + " Вау! Расскажи подробнее, это звучит очень интересно!",
		"sarcasm": "Хм, чувствую нотку сарказма. Расскажи, что думаешь на самом деле? 😏",
		"neutral": response
	};

	return moodResponses[userMood] || response;
}


// 🔹 Храним историю сообщений для каждого пользователя
let chatHistory = {};

// ✅ Функция определения смены темы беседы
function detectTopicChange(chatMessages) {
	if (!chatMessages || chatMessages.length < 3) return false;

	const lastUserMessage = chatMessages.at(-1).text.toLowerCase();
	const prevUserMessage = chatMessages.at(-2).text.toLowerCase();
	const prevPrevUserMessage = chatMessages.at(-3).text.toLowerCase();

	// Если последнее сообщение не похоже на предыдущие — тема изменилась
	if (lastUserMessage.length > 15 && 
		prevUserMessage.length > 15 && 
		!lastUserMessage.includes(prevUserMessage) && 
		!prevUserMessage.includes(prevPrevUserMessage)) {
		
		console.log("🔄 Тема беседы изменилась!");
		return true;
	}

	return false;
}


/**
 * 1. Начало обработки: получаем статус чата из БД, историю сообщений из iframe и инициализируем переменные.
 */
async function initProcessing(profileName) {
	//console.log(`📥 Обрабатываем сообщения ${profileName}...`);

	const { messages, stage: stage } = await getChatStatus(profileName);
	let prompt = "";
	let nextStage = stage;

	// Забираем свежие сообщения из iframe
	let chatMessages = await parseChatMessages() || [];

	return { messages, stage, chatMessages, prompt, nextStage };
}


/**
 * 2. Проверяем, не отправляли ли мы номер уже раньше (если да — уходим).
 */
function checkIfPhoneSentBefore(chatMessages) {
	if (isPhoneNumberSent(chatMessages)) { // ← Эта функция уже существует в вашем коде
		console.log("✅ Номер уже был отправлен.");
		return true;
	}
	return false;
}


/**
 * 3. Однократно пытаемся найти номер в сообщениях пользователя.
 */
async function parsePhoneFirstTime(chatMessages, profileName, userid) {
	const phoneFound = await parseAndSavePhoneNumber(chatMessages, profileName, userid); 
	return phoneFound;
}


/**
 * 4. Параллельно проверяем, грубит ли пользователь (isUserBeingRude) и ищем номер (parseAndSavePhoneNumber).
 */
async function checkPhoneAndRudeParallel(chatMessages, profileName, userid) {
	const [isRude, phoneFound] = await Promise.all([
		isUserBeingRude(chatMessages),            // ← уже есть в коде
		parseAndSavePhoneNumber(chatMessages, profileName, userid)
	]);
	return { isRude, phoneFound };
}


/**
 * 5. Если нашли номер - выходим, если пользователь грубит - добавляем в ЧС и выходим.
 */
async function handlePhoneOrRudeExit(phoneFound, isRude, profileName) {
	if (phoneFound) {
		console.log(`🚪 Выход из диалога с ${profileName}, так как номер уже найден.`);
		return true; // cигнализируем, что нужно прервать обработку
	}

	if (isRude) {
		console.warn(`🚨 Пользователь ${profileName} ведет себя неадекватно! Добавляем в ЧС.`);
		await addToBlacklist(profileName, "Оскорбления/неадекват"); // ← ваша существующая функция

		let iframeDoc = getIframeDocument();                     // ← уже есть в коде
		if (!iframeDoc) {
			console.warn("⚠️ iframe не найден!");
			return true; 
		}
		 // ← уже есть
		//await handleUser(iframeDoc, profileName);                   // ← уже есть
		await clickNearMenu();                                   // ← уже есть

		return true; // прерываем процесс
	}

	return false; // можно продолжать
}


/**
 * 6. Анализируем настроение (analyzeLastUserMessages) и параллельно сохраняем историю (saveChatStatus).
 */
async function updateHistoryAndAnalyzeMood(profileName, userid, oldMessages, stage, chatMessages) {
	const userMood = analyzeLastUserMessages(chatMessages); // ← ваша функция

	try {
		const updatedMessages = mergeUniqueMessages(oldMessages, chatMessages); // ← уже есть
		await saveChatStatus(profileName, userid, updatedMessages, stage);     // ← уже есть
	} catch (error) {
		console.error("❌ Ошибка при сохранении истории чата:", error);
	}

	return userMood;
}


/**
 * 7. Если пользователь уже согласился на WhatsApp (userMood === 'confirmed_telegram'),
 *    но бот ещё не отправлял контакт — отправляем его и выходим.
 */
async function handleConfirmedWhatsApp(userMood, chatMessages, profileName, userid, stage) {
	if (userMood === "confirmed_telegram" && !isPhoneAlreadySentByBot(chatMessages)) {
		console.log("✅ Пользователь согласился на WhatsApp, но контакт не отправлен. Отправляем номер...");
		let timestamp = new Date().toISOString();
		chatMessages.push({ user: "bot", text: WHATSAPP_CONTACT, time: timestamp });

		await Promise.all([
			saveChatStatus(profileName, userid, chatMessages, stage),
			handleSendMessageUserInput(WHATSAPP_CONTACT) // ← отправка сообщения
		]);

		return true; // прерываем обработку
	}
	return false;
}


/**
 * 8. Если пользователь спросил номер (isUserAskingForPhone), а мы его ещё не отправляли — отправляем.
 */
async function handleUserAskingForPhone(chatMessages, profileName, userid, stage) {
	const userAskedForPhone = isUserAskingForPhone(chatMessages);  // 🔍 Проверяем, запрашивал ли пользователь номер
	const lastBotMessage = getLastBotMessage(chatMessages);  // 📩 Берём последнее сообщение бота

	// ✅ Проверяем, отправлял ли бот уже номер
	const phoneAlreadySent = isPhoneNumberSent(chatMessages);

	if (userAskedForPhone && !phoneAlreadySent) {
		console.log("✅ Пользователь запросил номер телефона. Отправляем контакт...");

		let timestamp = new Date().toISOString();
		const botResponse = WHATSAPP_CONTACT;

		chatMessages.push({ user: "bot", text: botResponse, time: timestamp });

		await Promise.all([
			saveChatStatus(profileName, userid, chatMessages, stage),
			handleSendMessageUserInput(botResponse)
		]);

		return true;
	} 
	
	// ✅ Если номер уже отправлен, отвечаем иначе
	if (phoneAlreadySent && userAskedForPhone) {
		console.log("📌 Пользователь уже получил номер. Отправляем уточнение...");

		let timestamp = new Date().toISOString();
		const botResponse = "Напиши мне в WhatsApp, буду ждать! 😉";

		chatMessages.push({ user: "bot", text: botResponse, time: timestamp });

		await Promise.all([
			saveChatStatus(profileName, userid, chatMessages, stage),
			handleSendMessageUserInput(botResponse)
		]);

		return true;
	}

	return false;
}



/**
 * 9. Если бот уже отправил номер, но пользователь снова просит — даём уточнение "Напиши мне в WhatsApp".
 */
async function handleAlreadySentButAskedAgain(chatMessages, profileName, userid, stage) {
	const userAskedForPhone = isUserAskingForPhone(chatMessages);
	const phoneAlreadySent = isPhoneNumberSent(chatMessages);

	if (phoneAlreadySent && userAskedForPhone) {
		// В вашем исходном коде есть проверка: if (parseAndSavePhoneNumber === 1) { return; }
		// Сохраняем строку, не меняя:
		if (parseAndSavePhoneNumber === 1) {
			return true;
		}

		console.log("📌 Пользователь уже получил номер, уточняем...");
		const botResponse = "Напиши мне в WhatsApp, буду ждать! 😉";
		chatMessages.push({ user: "bot", text: botResponse, time: new Date().toISOString() });

		await Promise.all([
			saveChatStatus(profileName, userid, chatMessages, stage),
			handleSendMessageUserInput(botResponse)
		]);
		return true;
	}
	return false;
}


/**
 * 10. Если бот уже предлагал WhatsApp (hasBotOfferedTelegram) — выходим (return "NaN").
 */
function skipRepeatedWhatsAppOffer(chatMessages) {
	if (hasBotOfferedTelegram(chatMessages)) { // ← уже есть
		console.warn("⚠️ Бот уже предлагал WhatsApp, пропускаем повторное предложение.");
		return true;
	}
	return false;
}


/**
 * 11. Проверяем последнее сообщение на "написал", а также время ответа (canRespond).
 */
function checkLastMessageTime(chatMessages, oldMessages, stage) {
	const lastMessage = chatMessages.length > 0 ? chatMessages[chatMessages.length - 1] : null;
	const userMessages = chatMessages.filter(msg => msg.user === "user");

	const lastUserMessage = userMessages.length > 0
		? userMessages.reduce((latest, msg) => msg.time > latest.time ? msg : latest, userMessages[0])
		: { text: "(Нет сообщений от пользователя)" };



	//console.log("📩 Последнее сообщение от пользователя:", lastUserMessage.text);

	const lastMessageTime = lastMessage ? lastMessage.time : null;
	console.log("📌 lastMessageTime:", lastMessageTime);

	if (!lastMessageTime) {
		console.error("❌ Ошибка: lastMessageTime отсутствует или имеет неверный формат!", lastMessageTime);
		return { stopDialog: true };
	}

	const now = new Date();
	const lastTimeDate = new Date(lastMessageTime);

	if (isNaN(lastTimeDate.getTime())) {
		console.error("❌ Ошибка: неверный формат времени!", lastMessageTime);
		return { stopDialog: true };
	}

	const hours = lastTimeDate.getHours();
	const minutes = lastTimeDate.getMinutes();
	const lastTime = new Date(now.getFullYear(), now.getMonth(), now.getDate(), hours, minutes);
	console.log("✅ Время успешно преобразовано:", lastTime);

	if (!canRespond(lastTime, stage, lastUserMessage)) { // ← уже есть в коде
		console.warn("⏳ Время ответа истекло, сообщение не будет отправлено.");
		return { stopDialog: true };
	}

	return { stopDialog: false };
}


/**
 * 12. Определяем prompt в зависимости от стадии чата.
 *    (Стадии 1..4 и выше).
 */
function defineStagePrompt(stage, messages) {
	let prompt = "";

	if (stage === 1 && messages.length > 0) {
		console.log("⚠️ Уже есть сообщения, пропускаем приветствие");
	} else if (stage === 1) {
		prompt = "Сгенерируй приветственное сообщение.";
	} else if (stage === 2) {
		prompt = "Ответь кратко и задай естественный вопрос.";
	} else if (stage === 3) {
		prompt = "Предложить перейти в WhatsApp?";
	} else if (stage >= 4) {
		prompt = "Ждать пока сбросит свой номер или попросит мой. Если просит, нужно написать: `${WHATSAPP_CONTACT}`";
	}

	return prompt;
}


/**
 * 13. Если пользователь сам не задавал вопрос, убираем вопросительную часть из prompt (рег.выражение).
 */
function removeUnnecessaryQuestion(prompt, stage, chatMessages) {
	const botMessages = chatMessages.filter(msg => msg.user === "bot");
	const lastBotMessage = botMessages.sort((a, b) => new Date(a.time) - new Date(b.time)).pop();

	const userMessages = chatMessages.filter(msg => msg.user === "user");
	const lastUserMessage = userMessages.length > 0
		? userMessages.reduce((latest, msg) => msg.time > latest.time ? msg : latest, userMessages[0])
		: { text: "" };

	const lastUserText = (lastUserMessage?.text || "").toLowerCase();
	const userAskedQuestion = /[?？]$/.test(lastUserText) ||
		["что", "как", "где", "почему", "когда", "зачем"].some(word => lastUserText.includes(word));

	const shouldBotAskQuestion = userAskedQuestion || (stage === 1 && !botMessages.length);

	if (!shouldBotAskQuestion) {
		prompt = prompt.replace(/(Как|Что|Где|Почему|Когда|Зачем|Чем).*?\?/, "").trim();
		console.log("⚠️ Убираем лишний вопрос из промпта.");
	}

	return { prompt, lastBotMessage };
}


/**
 * 14. Генерация ответа от OpenAI с проверкой на дубликаты (до 3 попыток).
 */
async function generateUniqueResponse(prompt, profileName, userAge, stage, chatMessages, lastBotMessage, userid) {
	let botResponse = "";
	const MAX_RETRIES = 3;
	let attempt = 0;

	if (!prompt || prompt.trim().length === 0) {
		console.error("❌ Ошибка: `prompt` пустой. OpenAI не сможет ответить.");
		return ;
	}

	do {
		if (attempt > 0) {
			console.warn(`🔄 Попытка ${attempt} перегенерации сообщения...`);
		}

		try {
			botResponse = await generateMessage(profileName, userAge, prompt, "", stage, chatMessages, userid);
			console.log("!!!!!!!!!!!!!", botResponse);
		} catch (error) {
			console.error("❌ Ошибка в `generateMessage()`:", error);
			return;
		}

		attempt++;

		if (!botResponse || botResponse === "NaN") {
			console.warn("⚠️ OpenAI вернул некорректный ответ, пропускаем...");
			return;
		}

		// ✅ Вставляем `@NL@` случайно после приветствия и смайла
		botResponse = insertNewlineBeforeSecondSentence(botResponse);

		// ✅ Проверяем, не дублируется ли ответ
		if (lastBotMessage && isSimilarMessage(lastBotMessage.text, botResponse)) {
			console.warn("🚨 Бот уже отправлял такое сообщение, пробуем ещё раз.");
			botResponse = "";
			continue;
		}

		if (attempt >= MAX_RETRIES) {
			console.warn("⚠️ Не удалось сгенерировать уникальный ответ, отправляем fallback.");
			botResponse = "Интересно! А что ты думаешь об этом?";
		}

	} while (!botResponse);

	// ✅ Если ответ получен, заменяем "WhatsApp" → "вотcaп"
	botResponse = botResponse.replace(/WhatsApp/g, "вотcaп");

	return botResponse;
}

async function clearMimicMemory() {
	console.log("🧹 Очистка памяти в Mimic Browser...");

	// 1️⃣ Очистка локального хранилища и кэша
	localStorage.clear();
	sessionStorage.clear();
	caches.keys().then(names => names.forEach(name => caches.delete(name)));

	// 2️⃣ Очистка сервисных воркеров
	navigator.serviceWorker.getRegistrations().then(regs => regs.forEach(reg => reg.unregister()));

	// 3️⃣ Обнуление переменных
	window.userChatsCache = null;
	window.largeObject = null;

	// 4️⃣ Принудительный запуск GC
	if (window.gc) {
		console.log("🔄 Запускаем GC...");
		window.gc();
	}

	// 5️⃣ Вывод обновленного состояния памяти
	console.log("📡 Новое состояние памяти:", performance.memory);
}





function insertNewlineRandomly(response) {
	// ✅ 50% шанс вставить `@NL@`
	if (Math.random() < 0.5) {
		return response;
	}

	// ✅ Разбиваем текст на части, чтобы найти приветствие + смайлик
	const match = response.match(/^(Приветули|Привет|Приветики|Здорово|Хай|Хей|Йоу|Добрый день|Добрый вечер|Здравствуйте)[.!?]*[😊😉😜🤔😁👍✨❤️😄🎉]*\s*/);

	if (match) {
		const matchedText = match[0]; // Например: "Приветули! 😊 "
		const restOfResponse = response.slice(matchedText.length); // Остальной текст

		// ✅ Вставляем `@NL@` после приветствия и смайла
		return matchedText.trim() + " @NL@ " + restOfResponse;
	}

	return response;
}


function insertNewlineBeforeSecondSentence(response) {
	// ✅ Разбиваем текст на предложения, сохраняя знаки препинания
	const sentenceRegex = /([^.!?]+[.!?])/g;
	const parts = response.match(sentenceRegex);

	if (!parts || parts.length < 2) {
		return response; // Если только одно предложение — не меняем текст
	}

	// ✅ Выбираем случайные позиции для `@NL@`
	const insertIndexes = new Set();
	while (insertIndexes.size < Math.min(2, parts.length - 1)) {
		insertIndexes.add(Math.floor(Math.random() * (parts.length - 1)));
	}

	// ✅ Вставляем `@NL@` в выбранные места
	let result = "";
	for (let i = 0; i < parts.length; i++) {
		result += parts[i];
		if (insertIndexes.has(i)) {
			result += " @NL@";
		}
	}

	return result.trim();
}



/**
 * 15. Сохраняем новое сообщение бота, отправляем его пользователю и делаем логирование + паузу.
 *     Возвращаем новую стадию (nextStage).
 */
async function sendAndPause(botResponse, chatMessages, profileName, userid, stage, nextStage) {
	let timestamp = new Date().toISOString();
	chatMessages.push({ user: "bot", text: botResponse, time: timestamp });

	await Promise.all([
		saveChatStatus(profileName, userid, chatMessages, nextStage),
		handleSendMessageUserInput(botResponse)
	]);

	// Логи для отладки
	const userMessages = chatMessages.filter(msg => msg.user === "user");
	const lastUserMessage = userMessages.length > 0
		? userMessages.reduce((latest, msg) => msg.time > latest.time ? msg : latest, userMessages[0])
		: null;

	const botMessages = chatMessages.filter(msg => msg.user === "bot");
	const lastBotMessage = botMessages.length > 1 ? botMessages[botMessages.length - 2] : null; 

	console.log("📌 Последнее сообщение пользователя:", lastUserMessage?.text);
	console.log("📌 Последнее сообщение бота:", lastBotMessage?.text);
	console.log("📌 Запрос к OpenAI:", prompt);  // при желании можно вывести prompt
	console.log("📌 OpenAI ответ:", botResponse);

	await adaptivePause(botResponse); // ← уже есть

	// Логика повышения стадии
	if (stage === 2 && hasTelegramOffer(chatMessages)) {
		nextStage = 3;
	} else if (stage < MAX_STAGE - 1) {
		nextStage++;
	}

	return nextStage;
}


// ✅ Функция проверки и добавления пользователя
async function addUserIfNotExists(profileName, userName = "", registrationDate = "", userCity = "", userAge = "") {
	await window.waitForSupabase();

	let userInfo = await getUserInfo(profileName);

	if (!userInfo) {
		console.warn(`👤 Пользователь ${profileName} не найден в базе. Добавляем...`);
		await addUser(profileName, userName, registrationDate, userCity, userAge);
		userInfo = await getUserInfo(profileName); // Загружаем данные после создания
	} else {
		console.log(`✅ Пользователь уже есть в БД: ${profileName}`);
	}

	return userInfo;
}



// ✅ Проверка blacklist и добавление, если нужно
async function checkBlacklistAndDate(profileName, registrationDate) {
	await window.waitForSupabase();

	//==console.log(`🔍 [checkBlacklist] Проверяем blacklist для ${profileName}...`);

	// Проверяем, есть ли пользователь в blacklist
	const blacklisted = await isUserBlacklisted(profileName);
	if (blacklisted) {
		console.warn(`⛔ [checkBlacklist] ${profileName} уже в blacklist!`);
		return true; // Код выйдет из функции
	}

	// Проверяем дату регистрации
	const regDate = new Date(registrationDate);
	const currentDate = new Date();
	const registeredDays = Math.floor((currentDate - regDate) / (1000 * 60 * 60 * 24));

	//==console.log(`📅 [checkBlacklist] ${profileName} зарегистрирован ${registeredDays} дней назад (Порог: ${BLACKLIST_DAYS} дней)`);

	if (registeredDays > BLACKLIST_DAYS) {
		console.warn(`🚨 [checkBlacklist] ${profileName} добавляется в blacklist (старая регистрация)`);

		const added = await addToBlacklist(profileName, `Старая рега: ${registeredDays}`);
		if (!added) {
			console.error("❌ [checkBlacklist] Ошибка при добавлении в blacklist!");
			return true; // Прерываем выполнение
		}

		//==console.log(`✅ [checkBlacklist] ${profileName} успешно добавлен в blacklist.`);
		return true;
	}

	//==console.log(`✅ [checkBlacklist] ${profileName} проходит проверку blacklist.`);
	return false;
}


async function shouldProcessUser(profileName, currentuserid) {
	await window.waitForSupabase();

	try {
		// 1️⃣ Проверяем, есть ли чат в Supabase
		const { data, error } = await window.supabaseClient
			.from("chats")
			.select("userid")
			.eq("user_nick", profileName)
			.maybeSingle();

		// 2️⃣ Если ошибка — логируем и возвращаем false
		if (error) {
			console.error("❌ Ошибка при запросе чата:", error);
			return false;
		}

		// 3️⃣ Если чата нет — работаем дальше (не проверяем userid)
		if (!data) {
			console.log(`✅ Чат с ${profileName} отсутствует. Работаем дальше.`);
			return true;
		}

		// 4️⃣ Если userid совпадает — работаем дальше
		if (data.userid === currentuserid) {
			console.log(`✅ userid совпадает (${data.userid}). Работаем дальше.`);
			return true;
		}

		// 5️⃣ Если userid не совпадает — пропускаем пользователя
		console.warn(`🚨 userid НЕ совпадает! В базе: ${data.userid}, Текущий: ${currentuserid}. Пропускаем пользователя.`);
		return false;

	} catch (err) {
		console.error("❌ Фатальная ошибка в `shouldProcessUser()`:", err);
		return false;
	}
}

function findElementsByText_() {
	return elements;
}

/**
 * Извлекает данные о пользователе из Инфо пользователя.
 * @returns {Promise<{ userCity: string, userName: string, userAge: number | null, registrationDate: Date | null }>} Данные о пользователе.
 */
async function extractUserData() {
	await randomPause(1520, 2106);
	try {
		// Обновление контекста iframe
		//==console.log("Step 3: Refreshing iframe context and clicking 'Show more'...");
		const updatedIframe = document.querySelectorAll("iframe")[1];
		if (!updatedIframe) {
			throw new Error("❌ Не найден iframe");
		}
		const iframeDoc = updatedIframe.contentDocument || updatedIframe.contentWindow.document;
		if (!iframeDoc) {
			throw new Error("❌ Не удалось получить document внутри iframe");
		}
		await randomPause(120, 306);

		// Клик по кнопке 'Show more'
		const buttonSelector = "div.user_info_about_show_button.s__fwb.s__h3";
		try {
			const buttonElement = await waitForElementInIframe(updatedIframe, buttonSelector);
			safeClick(buttonElement, "'Показать полностью' button");
			
		} catch (error) {
			console.warn("'Show more' button not found. Skipping this step.");
		}
		await randomPause(120, 306);
		const selectors = {
			userCity: "div.user_info_header_info_user_city.s__h3 > a",
			userName: "div.user_info_header_info_user_name.s__fwb",
			userAge: "div.user_info_header_info_user_city.s__h3",
			registrationDate: "tr:nth-child(1) > td.user_info_about_additional_item_text"
		};


		let extractedData = {
			userCity: "❌ Не найден",
			userName: "❌ Не найден",
			userAge: null,
			registrationDate: null
		};

		try {
			let isBlocked = await waitForElementInIframe(updatedIframe, "div.user_info_blocked_date.s__fwb");
			if (isBlocked) {
				console.log("Пользователь в блоке: ", isBlocked);
				return extractedData, true;
			}
		} catch (error) {}


		for (const [key, selector] of Object.entries(selectors)) {
			try {
				const element = await waitForElementInIframe(updatedIframe, selector);
				if (!element) throw new Error(`${key} элемент не найден`);
				let value = element.textContent.trim();
				console.log(key, value);

				if (key === "userAge") {
					const ageMatch = value.match(/\d+/);
					extractedData[key] = ageMatch ? parseInt(ageMatch[0], 10) : null;
				} else if (key === "registrationDate") {
					const dateMatch = value.match(/(\d{2}\.\d{2}\.\d{4})/);
					if (dateMatch) {
						const [day, month, year] = dateMatch[1].split(".");
						extractedData[key] = new Date(`${year}-${month}-${day}`);
					} else {
						throw new Error("❌ Неверный формат даты");
					}
				} else {
					extractedData[key] = value;
				}
				console.log(`✅ Extracted ${key}:`, extractedData[key]);
			} catch (error) {
				console.log(`❌ Ошибка при извлечении ${key}:`, error);
			}
		}
		
		return extractedData;
	} catch (error) {
		console.error("❌ Ошибка в extractUserData:", error);
		return {
			userCity: "❌ Ошибка",
			userName: "❌ Ошибка",
			userAge: null,
			registrationDate: null
		};
	}
}



async function checkUserBehavior(chatMessages, profileName, userid) {
	const [phoneFound, rudeCheck] = await Promise.all([
		parsePhoneFirstTime(chatMessages, profileName, userid),
		checkPhoneAndRudeParallel(chatMessages, profileName, userid)
	]);
	return { phoneFound, isRude: rudeCheck.isRude };
}


/***************************************************
 * Функции для прокрутки и работы с интерфейсом профилей
 ***************************************************/
async function scrollPage() {
  let contentDiv = document.querySelector("#content");
  if (!contentDiv) {
	console.warn("⚠️ Не найден контейнер с контентом.");
	return false;
  }
  let previousHeight = contentDiv.scrollHeight;
  window.scrollBy(0, window.innerHeight);
  await randomPause(3000, 5000);
  let newHeight = contentDiv.scrollHeight;
  return newHeight > previousHeight;
}

/***************************************************
 * Функции обработки профилей
 ***************************************************/
// Функция обработки профилей (перебор элементов профилей)
async function processProfiles(maxProfiles = 50) {
  let profilesProcessed = 0;
  let hasMoreProfiles = true;
  let context, iframeDoc;

  while (hasMoreProfiles && profilesProcessed < maxProfiles) {
	context = getUpdatedIframeContext();
	if (!context) {
	  console.warn("⚠️ Ошибка получения iframe. Пропускаем итерацию...");
	  continue;
	}
	({ iframeDoc } = context);
	await randomPause(600, 1500);
	let myUrl = iframeDoc.querySelector("#content > div > hlink").getAttribute("value");
	console.log(myUrl);
	let { userid, password } = parseUrlParams(myUrl);
	console.log("🔍 Получили userid:", userid);
	
	let profileElements = iframeDoc.querySelectorAll(".people_near_content__item");
	if (profileElements.length === 0) {
	  console.warn("⚠️ Профили не найдены, возможно, страница не загружена.");
	  return;
	}

	// Используем индекс, чтобы при каждой итерации обновлять список
	for (let i = 0; i < profileElements.length && profilesProcessed < maxProfiles; i++) {
	  // Обновляем контекст и список профилей для текущей итерации
	  context = getUpdatedIframeContext();
	  if (!context) {
		console.warn("⚠️ Ошибка получения обновленного iframe. Пропускаем итерацию...");
		continue;
	  }
	  iframeDoc = context.iframeDoc;
	  profileElements = Array.from(iframeDoc.querySelectorAll(".people_near_content__item"));
	  let profile = profileElements[i];
	  if (!profile) break; // Если профиль не найден – выходим из цикла


	  //await randomPause(500, 1000);
	  let profileNickElement = profile.querySelector(".people_near_content__item__nick");
	  let profileName = profileNickElement.textContent.trim();
	  console.log(`📌 Обрабатываем профиль ${profileName}`);
	  let userCur = profile.getAttribute("data-id");
	  console.log(`📌 Обрабатываем userCur ${userCur}`);



	  // Проверяем, находится ли профиль в ЧС
	  let isBlacklisted = await checkBlacklist(profileName);
	  if (isBlacklisted) {
		console.warn(`⛔ Профиль ${profileName} в ЧС, пропускаем.`);
		continue;
	  }

	  // Проверяем, есть ли профиль в базе
	  let isUserInDatabase = await checkUserInfo(profileName);
	  if (!isUserInDatabase) {
		console.warn(`⚠️ Профиль ${profileName} отсутствует в БД, возможно, новый.`);
	  }


	  const userID = 82702089; 
	  const password = "9a0a8250cdd5db70a57df75d3cabfe40";
	  let { userCity, userName, userAge, registrationDate } = await fetchData(userCur, userID, password);
	  userAge = userAge.replace(/[^\d]/g, "");

	  if (userAge <= 25) {
		console.warn("⚠️ ⚠️ Молодой");
		continue;
	  }

	  await randomPause(900, 1500); 
	  isBlacklisted = await checkBlacklistAndDate(profileName, registrationDate)
	  if (!isBlacklisted) {		
		if (!context) {
		  console.warn("⚠️ Ошибка получения iframe. Пропускаем итерацию...");
		  continue;
		}
		({ iframeDoc } = context);
		
		// 🔥 Прокручиваем к нику профиля перед кликом
		profileNickElement.scrollIntoView({ behavior: "smooth", block: "center" });
		await randomPause(700, 900); // Ждем анимацию скролла
		profileNickElement.click();
		await randomPause(3500, 4900); // Ждем анимацию скролла
		await userProcessing(profileName, userid, userCity, userName, userAge, registrationDate);
		await clickNearMenu();
		await randomPause(1300, 1800);


		// Обновляем контекст сразу после fallback:
		context = getUpdatedIframeContext();
		iframeDoc = context ? context.iframeDoc : null;
	  }

	  await randomPause(900, 1900);
	  profilesProcessed++;
	}

	hasMoreProfiles = await scrollPage();
  }
  console.log(`✅ Обработано ${profilesProcessed} профилей.`);
}



/**
 * Функция для получения данных пользователя с сервера.
 * 
 * @param {number|string} userCur - Идентификатор текущего пользователя.
 * @param {number|string} userID - Идентификатор запрашиваемого пользователя.
 * @param {string} password - Пароль для запроса.
 * @returns {Promise<Object>} Объект с данными: userCity, userName, userAge, registrationDate, userBlocked.
 */
async function fetchData(userCur, userID, password) {
  // Генерация случайного значения, аналог random.uniform в Python.
  const randomValue = (Math.random() * (0.999999 - 0.000001) + 0.000001).toFixed(17);

  // Формирование URL с подстановкой параметров с использованием шаблонных строк.
  const url = `https://galaxy.mobstudio.ru/services/?userID=${userID}&password=${password}&a=whois&usercur=${userCur}&from=people_near&random=${randomValue}`;

  // Заголовки запроса, аналогичные Python-словарю headers.
  const headers = {
	"accept": "*/*",
	"accept-language": "en,ru;q=0.9,en-US;q=0.8",
	"dnt": "1",
	"priority": "u=1, i",
	"referer": "https://galaxy.mobstudio.ru/web/",
	"sec-ch-ua": `"Chromium";v="134", "Not:A-Brand";v="24", "Google Chrome";v="134"`,
	"sec-ch-ua-mobile": "?0",
	"sec-ch-ua-platform": `"Windows"`,
	"sec-fetch-dest": "empty",
	"sec-fetch-mode": "cors",
	"sec-fetch-site": "same-origin",
	"user-agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.0.0 Safari/537.36",
	"x-galaxy-client-ver": "9.5",
	"x-galaxy-kbv": "352",
	"x-galaxy-lng": "ru",
	"x-galaxy-model": "chrome 133.0.0.0",
	"x-galaxy-orientation": "portrait",
	"x-galaxy-os-ver": "1",
	"x-galaxy-platform": "web",
	"x-galaxy-scr-dpi": "0.6666666865348816",
	"x-galaxy-scr-h": "1207",
	"x-galaxy-scr-w": "700",
	"x-galaxy-user-agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/134.0.0.0 Safari/537.36"
  };

  // XPath-селекторы для извлечения данных из HTML.
  // Для userBlocked используется примерный селектор — его можно скорректировать под реальные данные.
  const xpaths = {
	userCity: "//div[contains(@class, 'user_info_header_info_user_city') and contains(@class, 's__h3')]/a/text()",
	userName: "//div[contains(@class, 'user_info_header_info_user_name') and contains(@class, 's__fwb')]/text()",
	userAge: "//div[contains(@class, 'user_info_header_info_user_city') and contains(@class, 's__h3')]/text()",
	registrationDate: "//tr[1]/td[contains(@class, 'user_info_about_additional_item_text')]/text()"
	// userBlocked: "//div[contains(@class, 'user_blocked')]/text()"  // Пример XPath, можно заменить
  };

  try {
	// Выполнение GET-запроса через Fetch API.
	const response = await fetch(url, { headers });

	// Если ответ не успешный, выбрасываем ошибку (аналог response.raise_for_status() в Python).
	if (!response.ok) {
	  throw new Error(`Ошибка запроса: ${response.status} ${response.statusText}`);
	}

	// Получаем Content-Type ответа.
	const contentType = response.headers.get("Content-Type") || "";

	if (contentType.includes("application/json")) {
	  // Если ответ в формате JSON — парсим и возвращаем его.
	  const data = await response.json();
	  return data;
	} else {
	  // Если ответ в формате HTML, получаем текст ответа.
	  const text = await response.text();
	  // Используем DOMParser для парсинга HTML-строки.
	  const parser = new DOMParser();
	  const doc = parser.parseFromString(text, "text/html");

	  // Функция для выполнения XPath-запроса и получения строки результата.
	  function getXPathResult(doc, xpath) {
		// document.evaluate возвращает XPathResult, используем STRING_TYPE для получения строкового значения.
		const result = doc.evaluate(xpath, doc, null, XPathResult.STRING_TYPE, null);
		return result.stringValue.trim();
	  }

	  // Составляем объект с результатами по каждому XPath.
	  const result = {};
	  for (const [key, path] of Object.entries(xpaths)) {
		let value = getXPathResult(doc, path) || '';

		// Для registrationDate удаляем префикс "Дата регистрации:".
		if (key === "registrationDate") {
		  value = value.replace("Дата регистрации:", "").trim();
		}
		// Для userAge удаляем суффикс " года" (оставляем только число).
		if (key === "userAge") {
		  value = value.replace(" года", "").trim();
		}
		result[key] = value;
	  }
	  return result;
	}
  } catch (error) {
	console.error("Ошибка запроса:", error);
	throw error;
  }
}

/* Пример вызова функции (в chrome console или в асинхронном контексте):
(async () => {
  const userCur = 82859343;
  const userID = 82702089;
  const password = "9a0a8250cdd5db70a57df75d3cabfe40";

  try {
	const { userCity, userName, userAge, registrationDate, userBlocked } = await fetchData(userCur, userID, password);
	console.log("userCity:", userCity);
	console.log("userName:", userName);
	console.log("userAge:", userAge); // Ожидается, например: '23'
	console.log("registrationDate:", registrationDate); // Ожидается, например: '30.01.2025'
	console.log("userBlocked:", userBlocked);
  } catch (error) {
	console.error("Ошибка при получении данных:", error);
  }
})();
*/



// Функция обработки пользователя (извлечение данных, проверка, отправка сообщения)
async function userProcessing(profileName, userid, userCity, userName, userAge, registrationDate) {
  let context = getUpdatedIframeContext();
  if (!context) {
	console.warn("⚠️ iframe не найден после правого клика! Пропускаем...");
  }
  let { iframeDoc } = context;
  await randomPause(600, 996);
  console.log("📊 Извлекаем данные о пользователе...");
  //const userCur = 82859343;

  //const { userCity, userName, userAge, registrationDate, userBlocked } = await extractUserData();


  console.log("Город:", userCity);

  // if (userBlocked) {
  //   console.log("Пользователь заблокирован");
  //   await addToBlacklist(profileName, "Пользователь заблокирован");
  //   return;
  // }

  if (userCity !== "❌ Не найден") {
	const isRussian = await isCityInDatabase(userCity);
	console.warn(`🚨 Проверка города:`, isRussian);
	if (!isRussian) {
	  console.warn(`🚨 Город не в РФ. Добавляем в ЧС.`);
	  await addToBlacklist(profileName, `Город не в РФ: ${userCity}`);
	  await clickNearMenu();
	  //await handleUser(iframeDoc, profileName);
	  return;
	}
  }

  await addUserIfNotExists(profileName, userName, registrationDate, userCity, userAge);

  if (registrationDate) {
	const currentDate = new Date();
	const registeredDays = Math.floor((currentDate - registrationDate) / (1000 * 60 * 60 * 24));
	if (registeredDays > BLACKLIST_DAYS) {
	  console.warn(`🚨 Пользователь '${profileName}' зарегистрирован ${registeredDays} дней назад. Добавляем в ЧС.`);
	  await addToBlacklist(profileName, `Старая рега: ${registeredDays}`);
	  //await clickFallback();
	  //await handleUser(iframeDoc, profileName);
	  return;
	}
  }

  console.log(`💬 Отправка сообщения пользователю '${profileName}'...`);
  await clickMessageButton();

  const isLocked = await lockUserProcessing(profileName);
  if (!isLocked) {
	console.log("⚠️ Пропускаем пользователя, он уже обрабатывается в другом браузере...");
	//await clickFallback();
	return;
  }

  try {
	// Здесь вызывается processUserMessage(), которая обрабатывает диалог уже через историю сообщений
	// Если такая логика нужна, оставляем вызов; можно заменить на свою обработку
	await processUserMessage(profileName, userCity, userAge, userid);
	//await clickFallback();
	await clickNearMenu();
  } finally {
	await unlockUserProcessing(profileName);
  }
}
/***************************************************
 * Функции работы с профилями и пользователями
 ***************************************************/
// Проверка наличия пользователя в blacklist (используется при обработке профилей)
async function checkBlacklist(profileName) {
  await window.waitForSupabase();
  try {
	const { data, error } = await window.supabaseClient
	  .from("blacklist")
	  .select("user_nick")
	  .eq("user_nick", profileName)
	  .maybeSingle();
	if (error) return false;
	return data !== null;
  } catch (err) {
	console.error("❌ Ошибка проверки ЧС:", err);
	return false;
  }
}

// Проверка наличия пользователя в базе
async function checkUserInfo(profileName) {
  await window.waitForSupabase();
  try {
	const { data, error } = await window.supabaseClient
	  .from("user_info_galaxy")
	  .select("user_nick")
	  .eq("user_nick", profileName)
	  .maybeSingle();
	if (error) {
	  console.error("❌ Ошибка проверки user_info:", error.message);
	  return false;
	}
	return data !== null;
  } catch (err) {
	console.error("❌ Фатальная ошибка при проверке user_info:", err);
	return false;
  }
}


/***************************************************
 * Основной цикл (Main Loop)
 ***************************************************/
// Вместо обработки отдельных сообщений мы обрабатываем профили
(async () => {
  let profileName = "";

  try {
	console.log("⏳ Загрузка Supabase перед началом итераций...");
	await waitForSupabase();
	if (!window.supabaseClient) {
	  console.error("❌ Supabase client не инициализирован.");
	  return;
	}
	let context = getUpdatedIframeContext();
	if (!context) {
		console.warn("⚠️ iframe не найден, выходим...");
		return;
	}
	let { iframeDoc } = context;
	// Основной цикл итераций для обработки профилей
	let iterationCount = 0;
	for (let i = 0; i < ITER; i++) {
	  try {
		  document.querySelector("#root > div > div.auth-user > aside > div.mdc-drawer__content.drawer__content.scroll > nav > a:nth-child(2)").click();
		  await randomPause(900, 1336);

	  } catch (error) {};
	 
	  if (iterationCount >= 10) {
		console.log("Очистка памяти...");
		// Функция очистки памяти (при необходимости)
		// clearMimicMemory();
		iterationCount = 0;
		await randomPause(500, 836);
	  }
	  await processProfiles(50);
	  //await clickFallback();
	  iterationCount++;
	  break;
	  await randomPause(700, 1836);
	}
	console.log("🎉 Все итерации обработки профилей завершены!");
  } catch (error) {
	console.error("❌ Ошибка в основном цикле:", error);
  }
})();
